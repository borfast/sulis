# Security Hardening v1 — Progress

**This is the state file. Read it first, update it in every commit.**

Plan: `PLAN.md` (same directory) · Audit: `docs/security-audit-2026-08-17.html`

---

## Session protocol

### Starting a session

1. Read this file. Do not read `PLAN.md` end to end — it is long.
2. Run `git log --oneline -5` and `git status`. If they disagree with "Current position" below, trust git and fix this file first.
3. Find the first task not marked `[x]`. Read only that task's section in `PLAN.md`.
4. Read "Ratified API" and "Decisions" below. They override `PLAN.md` Appendix A where they differ.
5. Start work.

### During a session

Update this file **in the same commit as the code**. Never commit code without updating it — a commit that advances the work but leaves this file stale is what breaks the next session.

On every commit:
- Tick the task's checkbox, or set its status to `in-progress`.
- Update "Current position".
- Add a line to "Session log".
- Add any API or design decision to "Decisions". If it contradicts `PLAN.md` Appendix A, say so explicitly.

### Ending a session

Leave the tree in one of two states, and say which in "Current position":

- **GREEN** — `go build ./... && go vet ./... && go test -race ./...` all pass. Preferred.
- **RED (mid-TDD)** — a deliberately failing test exists. Name the exact test and the expected failure in "Current position", so the next session knows the red is intentional.

Never end a session with uncommitted work. If a task is half done, commit it as `in-progress` with a note describing what is done and what is next.

### Verification command

```
go build ./... && go vet ./... && go test -race -count=1 ./...
```

After T001, also: `gofmt -l .`, `staticcheck ./...`, `gosec ./...`.

### Commit style

Imperative subject, no type prefix, body wrapped at ~72 columns explaining *why*. No trailers.

```
Gate new sessions on verified email by default

Add RequireVerifiedEmail (default true) and ErrEmailNotVerified, gating
Login, IssueSession, CreateTwoFactorToken, and CompleteTwoFactor on
EmailVerifiedAt. Register and magic-link redemption stay exempt.
```

---

## Current position

**Branch:** `security-hardening-v1` (branched from `main` at `bf18c6e`). All work happens here; not yet pushed.
**Status:** T001, T002, T101–T107, T201–T206, T301–T305, T401–T402, T501–T504 done. **Phase 5 in progress (4/10).**
**Next task:** T505 — password quality beyond length.
**Tree:** GREEN. `gofmt`, `go build`, `go vet`, `staticcheck`, `gosec`, and `go test -race -count=1 ./...` all pass locally (this session ran staticcheck/gosec via `go run <module>@<pinned version>`, since neither binary is on `PATH`; `govulncheck` was not re-run this session).
**Coverage** (re-measured at T206; `totp` re-measured at T302; root re-measured at T305; all four re-measured at T402; root re-measured at T501, T502, T502 fix round 1, T503, T503 fix round 1, and T504): `passkey` **98.3%** (unchanged), root `sulis` **88.4%** (up from 88.2% — `needsRehash` and `rehashPassword` are both fully exercised, including the guard and error-swallowing branches), `recovery` **93.0%** (unchanged), `totp` **89.7%** (unchanged). `passkey` was the module's weakest package and is now its strongest. The three fuzzed-package bumps at T402 are the checked-in fuzz seed corpora themselves running as regression subtests under plain `go test` — error branches in `decodeHash`/`generateCode` (wrong algorithm/version, out-of-range params, malformed base32) that no hand-written test happened to hit before now do. T401 added two packages: `memstore` is 100% covered by the `storetest` suite it is run against (still 100% after T503); `storetest` itself carries no tests of its own by design (it *is* the tests).
**Blockers:** None. CI has not run on GitHub yet — the branch is unpushed, so the workflow is verified locally only. Push and confirm the workflow is green before relying on it.

**Carry-forward for whoever picks this up:**
- All 26 completed tasks are committed; the working tree is clean apart from pre-existing untracked files (`.claude/`, `.codegraph/`, `.gitnexus/`, `AGENTS.md`, `CLAUDE.md`). (The "16" this bullet used to claim was stale; the count is T001–T002, T101–T107, T201–T206, T301–T305, T401–T402, T501–T504.)
- **Raising `Argon2Params` now upgrades stored hashes transparently as users log in** (T504): new unexported `needsRehash(encoded string, want Argon2Params) bool` in `password.go` decodes a stored hash's params (`decodeHash`, unchanged) and compares only the three Argon2 cost dimensions — memory, iterations, parallelism — against `want`; SaltLength/KeyLength are fixed by the hash that already exists and are not compared. A decode failure returns `false` (no rehash), since `verifyPassword` already rejects a malformed hash before `needsRehash` is ever reached. New unexported `(*Sulis).rehashPassword(ctx, user, password)` in `sulis.go`, called from `VerifyPassword` immediately after the `if !ok` correctness check (so only a *successful* verification can trigger it — a wrong password, unknown email, or passwordless account never does, leaving the timing-equalization story unchanged) and only when `needsRehash(user.PasswordHash, s.cfg.Argon2)` is true: it re-derives the hash from the just-verified plaintext with `s.cfg.Argon2` (the CONFIGURED params, not the stored ones) and writes it through the existing `updateUserWithRetry`, whose mutate callback re-checks on every attempt that `u.PasswordHash` still equals the hash that was just verified before overwriting it — the same discipline `ChangePassword`'s `verifyOld` guard uses — so a password changed by a concurrent request between the read and the write is never clobbered by a rehash of the password it just replaced (guard failure returns the new unexported sentinel `errRehashPasswordChanged`, which never escapes `rehashPassword`). Every failure path in `rehashPassword` (guard loss, exhausted `ErrConcurrentUpdate` retries, any other store error, even `hashPassword` itself failing) is swallowed: the caller already authenticated correctly by the time this runs, so only the cost of the *next* login is at stake, never this one's correctness. Three `// TODO(T509): emit rehash event` markers (success, guard-loss/store-error, and hash-failure paths) are left for the security event sink task rather than inventing a second event mechanism now. No public signature changed on either `VerifyPassword` or `Login`. TDD: five failing tests written first against the unchanged `password.go`/`sulis.go` (`TestNeedsRehash` in `password_test.go`; `TestLoginUpgradesWeakStoredHash`, `TestLoginRehashRetriesOnConcurrentUpdateConflict`, `TestFailedLoginNeverRehashes`, `TestLoginRehashWriteFailureDoesNotFailLogin`, `TestLoginDoesNotRehashWhenStoredParamsAreStrongEnough`, `TestRehashDoesNotClobberConcurrentPasswordChange` in `sulis_test.go`) — confirmed red via `go vet` compile failure (`undefined: needsRehash`), this series' established compile-failure-as-red convention — then implemented. New test fixtures: `weakerArgon2Params` (next to the existing `testArgon2Params`, deliberately weaker still — the minimum memory `decodeHash` accepts — so "an operator raised `Argon2Params`" is reproducible cheaply with two `Sulis` instances sharing one store rather than a second real hashing-cost tier) and two new store-double types mirroring the existing `failUpdateUserStore`/`touchCountingSessionStore` pattern: `updateCountingUserStore` (wraps `memUserStore`, counts `UpdateUser` calls, proves the "stored params already strong enough" path writes nothing at all — not even a no-op) and reuse of `failUpdateUserStore` for the best-effort-write test. The concurrent-update-conflict test reuses the established `beforeUpdate`-hook interleaving technique (`memUserStore`) to land an unrelated write between the rehash's read and its write, asserting the retry both preserves the interleaved write and still lands the upgrade (`Version` advances by at least 2, not by a bare overwrite); a second interleaving test lands a complete concurrent `ChangePassword` in the same window and asserts the rehash backs off rather than resurrecting the password just replaced — the regression test for the binding TOCTOU constraint. Mutation-tested three properties: (1) `needsRehash` forced to always return `false` — both the upgrade test and the concurrent-retry test failed exactly as predicted (the upgrade test on its own sanity guard, since a rehash that never fires can't be retried either); (2) the rehash call moved before the `if !ok` check so a failed login rehashes too — `TestFailedLoginNeverRehashes` failed exactly as predicted; (3) the guard's `u.PasswordHash != verifiedHash` condition short-circuited with `false &&` — `TestRehashDoesNotClobberConcurrentPasswordChange` failed exactly as predicted (the old password still verified after the concurrent change). All three reverted after confirming; `go build`/`go vet`/`go test` clean afterward each time. `gitnexus impact` (upstream, `summaryOnly`) on `VerifyPassword` before editing: 1 direct dependent (`Login`), LOW risk — no HIGH/CRITICAL warning needed. `gitnexus detect_changes` (scope `unstaged`) afterward reported `risk_level: high` (10 changed symbols, 4 files); verified via `git diff` that `decodeHash`, `ChangePassword`, `SetInitialPassword`, and `checkPasswordPolicy` are byte-for-byte unchanged — the same line-shift false-positive pattern documented for this tool at T303/T304/T305/T402/T501/T502/T503 (each sits below a real insertion in the same file); the genuinely-changed symbols are exactly `VerifyPassword`'s new rehash call and the new `password_test.go`/`sulis_test.go` tests. README gained a paragraph under "Login and VerifyPassword" documenting the upgrade path and its best-effort write. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 6 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues). Root `sulis` coverage 88.2% → 88.4%; `memstore`/`passkey`/`recovery`/`totp` unchanged. **Phase 5: 4/10 tasks done.**
- **Sessions are now listable, refreshable, and can idle-expire** (T503): `Session` gains `LastSeenAt time.Time`, `IdleExpiresAt *time.Time`, `IP string`, `UserAgent string`. `SessionStore` gains `ListUserSessions(ctx, userID) ([]Session, error)`, `TouchSession(ctx, id, lastSeen, idleExpires *time.Time) error`, and `DeleteUserSessionsExcept(ctx, userID, keepSessionID) error`. New `Sulis` methods: `ListUserSessions(ctx, userID) ([]Session, error)` (strips `TokenHash` to `""` on every returned session — the security property this task exists for) and `RefreshSession(ctx, session) (*Session, string, error)` (rotates the token onto a **new session ID** — decided over reusing the same ID, since the store has no in-place token/expiry update primitive and reusing `CreateSession`+`DeleteSession` avoids adding one for a single caller; see the Decisions row — while preserving `UserID`/`Method`/`AuthenticatedAt`/`CreatedAt`/`IP`/`UserAgent`/`Metadata`, deliberately **not** refreshing `AuthenticatedAt`: a token rotation is not a fresh authentication proof). `WithIdleTimeout(d)` new `Option`, zero (default) disables idle expiry entirely. `ValidateSession` now checks `IdleExpiresAt` (before `ExpiresAt`, same `ErrSessionExpired` outcome either way) and, on success, refreshes `LastSeenAt`/`IdleExpiresAt` via `TouchSession` — but **throttled**: only once the session's current `LastSeenAt` is older than `sessionTouchInterval()` (`IdleTimeout/4` when idle expiry is configured, else a fixed 5 minutes), not on every call, or every authenticated request would cost a store write. `RequestInfo` now reaches `Session.IP`/`UserAgent`: `createSession` grew a `ri RequestInfo` parameter threaded from `Register`, `completeFirstFactor` (`Login`/`RedeemMagicLink`), and `CompleteTwoFactor`; `IssueSession`/`IssueSessionUnchecked` keep their unchanged Appendix A signatures (no `ri` parameter) and so mint sessions with zero-value `IP`/`UserAgent` — see the Decisions row. Also closed the T501 Deferred gap assigned here as a candidate: `ReAuthenticate` now calls `accountStatus` too, so it can no longer refresh `AuthenticatedAt` for a disabled/locked account's already-held session. `storetest/session.go` extended with ten new subtests for the three new store methods and the two new fields; `memstore.SessionStore` implements all three (`cloneSession` now also clones `IdleExpiresAt`); the three in-package `sulis_test.go` session-store doubles (`memSessionStore`, `observingSessionStore`, `sharedSessionStore`) updated too. README gained a "Session visibility and lifecycle" Core Flows section, an idle-timeout/touch-throttle paragraph under "ValidateSession", and the `SessionStore` Store Contracts bullet documents `ListUserSessions`/`TouchSession`/`DeleteUserSessionsExcept` in house MUST/SQL style. See the T503 Decisions rows for the full reasoning on the new-ID choice, the `RequestInfo` deviation, and why there's no `Sulis`-level wrapper for `DeleteUserSessionsExcept`.
- **Accounts can now be taken out of service, and there is an opt-in automatic lockout** (T502): new `status.go` — `DisableUser(ctx, userID, reason)` stamps `User.DisabledAt`/`DisabledReason` and revokes every session for the account; `EnableUser(ctx, userID)` reverses it. `User` gains `DisabledAt *time.Time`, `DisabledReason string`, `LockedUntil *time.Time`, `FailedLoginAttempts int` — plain fields round-tripped through the existing `UserStore` methods, no interface change (per the T002 Decision #2 rationale that `Version` exists precisely so fields like these don't force interface churn). `accountStatus(user)` is the shared check (`ErrAccountDisabled` if `DisabledAt` set, `ErrAccountLocked` if `LockedUntil` is still in the future) called from every session-issuance path: `VerifyPassword` (after the password verifies — see the oracle row below), `completeFirstFactor` (the `Login`/`RedeemMagicLink` choke point), `issueSessionForUser` (`IssueSession`/`IssueSessionUnchecked`), and `CompleteTwoFactor`. `ValidateSession` checks `DisabledAt` directly (not through `accountStatus`, and deliberately not `LockedUntil` — see the row below) so an already-issued session dies on its very next use once the account is disabled, independent of whether `DisableUser`'s own session revocation succeeds. New sentinels `ErrAccountDisabled`/`ErrAccountLocked` match Appendix A's ratified messages exactly (`"sulis: account disabled"` / `"sulis: account locked"`). Optional automatic lockout: `WithFailureLockout(threshold, baseBackoff, maxBackoff)`, default **off** (`FailureLockoutThreshold` 0) — `VerifyPassword`'s wrong-password branch calls `recordFailedLogin` (best-effort, swallows store errors) only when configured, incrementing `FailedLoginAttempts` and setting/extending `LockedUntil` via the pure `lockoutBackoff(base, max, excess)` helper (doubling per failure past threshold, capped, overflow-safe); the success branch calls `clearFailedLogins` once `accountStatus` has confirmed the account isn't currently locked. `storetest/user.go` extended in the same commit with a new `DisableAndLockFieldsRoundTripAndAreNotAliased` subtest (round-trip, pointer-aliasing, and clear-back-to-nil, mirroring `EmailVerifiedAt`'s existing coverage) plus `DisabledReason`/`FailedLoginAttempts` added to `assertUserMatches`; `memstore.cloneUser` and `sulis_test.go`'s `cloneTestUser` both clone the two new pointer fields. README gained an "Account disable and lockout" section. See the T502 Decisions rows for the field-vs-Metadata choice, the `ValidateSession`-checks-only-`DisabledAt` split, the store-shape choice for failure counting, and the `twofactor.go`/`stepup.go` scope calls.
- **Sessions now carry `AuthenticatedAt`/`Method`, and step-up auth is real** (T501): `RequireRecentAuth(ctx, session, maxAge)` / `ReAuthenticate(ctx, session, password, ri)` per Appendix A, new `stepup.go`. `SessionStore` gained `UpdateAuthenticatedAt(ctx, id, at)` — a dedicated method, not a parameter bolted onto T503's forthcoming `TouchSession`; see the T501 Decisions row for why the two must stay separate. `createSession` now takes `method AuthMethod` and `authenticatedAt time.Time`, threaded from every call site (`Register`, `completeFirstFactor`, `CompleteTwoFactor` — which always records `AuthMethodTwoFactor`, not the first factor's method — and `issueSessionForUser`, which for once actually reads `Authentication.at`, unused since T305). `totp.Service.Enroll`/`ReplaceEnrollment`'s "forthcoming `RequireRecentAuth`" GoDoc forward-reference now points at the real thing. README gained a "Step-up authentication" section naming every operation that should be gated (TOTP enroll/replace, passkey add/remove, 2FA disable, email change, recovery-code regeneration).
- **All four hand-rolled parsers are now fuzzed** (T402): `FuzzDecodeHash`/`FuzzNormalizeEmail` (root `fuzz_test.go`), `FuzzRecoveryCanonical` (`recovery/fuzz_test.go`), `FuzzGenerateCode` (`totp/fuzz_test.go`). Each asserts a real property (no-panic, round-trip, or idempotency — see the T402 Decisions row for which), not just "doesn't crash". Fuzzing found one genuine bug: `recovery.canonical` was not idempotent for input mixing dashes with non-space whitespace (e.g. a tab). Fixed, with a checked-in crasher regression seed (`recovery/testdata/fuzz/FuzzRecoveryCanonical/`) and a named unit test. CI gained a `fuzz` job running a 10s smoke pass per target.
- **There is now a store conformance suite** (T401): `storetest.Run*` holds any implementation of the seven store interfaces to the contracts written across T101–T305, and `memstore` is the reference implementation that passes it. Two consequences for later tasks. First, when a task changes a store interface's contract, the corresponding `storetest` subtest is where the new requirement gets pinned — prose in the GoDoc alone is now the weaker half of the job. Second, T601/T602's SQLite and Postgres stores should be built against `storetest` from the first commit rather than hand-tested; the suite is what "compliant" means. Note that `storetest`/`memstore` cannot be imported from any of this module's existing test files (import cycle — see the T401 Decisions row).
- **The two passkey test debts are discharged** (T206). T105's UV debt: `TestFinishLoginRejectsAssertionWithoutUserVerification` (plus its discoverable-login and registration counterparts) now rejects a UV-absent assertion end to end. T205's `ValidateLogin` debt: `TestLoginSucceedsForBackupEligibleCredential` drives a real signed assertion through `wa.ValidateLogin` against a backup-eligible stored credential. Both were mutation-tested; see the T206 Session log entry.
- **There is now a forging test authenticator** in `passkey/testauth_test.go` (`testAuthenticator` + `forgingFixture`). Reach for it before writing any new passkey ceremony test: it mints signed registration and assertion responses with controllable flags (UP/UV/BE/BS), RP ID, origin, challenge, sign count, user handle, credential ID, and signing key. Adding a rejection-path test is now a few lines, not a fixture-engineering exercise.
- Every finding fixed so far was mutation-tested — the regression test was confirmed to fail when the fix is reverted. Keep doing that for the security-critical ones; a test that cannot fail is decoration.
- T201's TDD red was unusually informative: running the new concurrency test against the pre-fix get-then-`defer`-delete code under `-race` didn't just fail the logical assertion (0 of 2 racers got `ErrChallengeExpired` — both proceeded past the "expired" gate), it also tripped the race detector on the un-mutexed in-memory test double's map (concurrent read/delete). Both symptoms point at the same underlying bug; see the T201 Session log entry.

---

## Task list

38 tasks, 7 phases. `[ ]` todo · `[~]` in progress · `[x]` done.

### Phase 0 — Foundations
- [x] **T001** · D2 · Harden CI (pin tools, scope token, add staticcheck/gosec/gofmt gates)
- [x] **T002** · Ratify the target API surface into "Ratified API" below

### Phase 1 — Close the bypasses
- [x] **T101** · A3 · Stop whole-row user writes resurrecting old credentials
- [x] **T102** · A1 · Make session issuance aware of second factors
- [x] **T103** · A1 · Close the magic-link 2FA bypass
- [x] **T104** · B5 · Remove `Session.Token`
- [x] **T105** · A2 · Require WebAuthn user verification
- [x] **T106** · B1 · Rate limiting on by default, with an IP dimension
- [x] **T107** · C1 · Own the email-change flow

### Phase 2 — Passkey hardening
- [x] **T201** · A4 · Atomic challenge consumption
- [x] **T202** · A5 · Populate `excludeCredentials` from the store
- [x] **T203** · A6 · Request resident keys
- [x] **T204** · A7 · Bound the ceremony body, decouple from `net/http`
- [x] **T205** · C12 · Record credential metadata
- [x] **T206** · D3 · Bring passkey coverage to parity

### Phase 3 — Safe defaults
- [x] **T301** · B4 · `totp.Validate` returns `error` only
- [x] **T302** · A8 · `totp.Enroll` cannot clobber a verified factor
- [x] **T303** · B3 · `RevokeSession` takes a user ID
- [x] **T304** · B2 · `CreatePasswordResetToken` stops leaking existence
- [x] **T305** · B6 · Make "already authenticated" a type

### Phase 4 — Store contracts and fuzzing
- [x] **T401** · D1 · Publish a store conformance suite
- [x] **T402** · D4 · Fuzz the parsers

### Phase 5 — Complete the feature set
- [x] **T501** · C7 · Step-up authentication
- [x] **T502** · C5 · Account disable and lockout
- [x] **T503** · C10 · Session visibility and lifecycle
- [x] **T504** · C3 · Rehash on login
- [ ] **T505** · C2 · Password quality beyond length
- [ ] **T506** · C4 · Encrypt TOTP secrets; offer a pepper
- [ ] **T507** · C6 · Cookie and CSRF helpers
- [ ] **T508** · C8 · Split magic-link TTL; bind links to the requester
- [ ] **T509** · C9 · Security event sink
- [ ] **T510** · C11 · Wire recovery codes into the 2FA lifecycle

### Phase 6 — Reference stores
- [ ] **T601** · D1 · SQLite reference store
- [ ] **T602** · D1 · Postgres reference store

### Phase 7 — Release readiness
- [ ] **T701** · D5 · `SECURITY.md` and threat model
- [ ] **T702** · D6 · Package docs and examples
- [ ] **T703** · D7 · Contribution, changelog, versioning
- [ ] **T704** · Final verification sweep

---

## Ratified API

**Ratified 2026-08-18 (T002): `PLAN.md` Appendix A is adopted as written, with the four questions answered below.**

Appendix A stays the single source of truth for signatures — it is not copied here, so there is nothing to drift. Any later amendment goes in the Decisions table below, marked as overriding Appendix A.

| # | Question | Decision | Reason |
|---|---|---|---|
| 1 | `RequestInfo` explicit param or context value? | **Explicit param** | Per-IP rate limiting must be visible in the signature. A context value is silently droppable, and the failure is invisible — the limiter just never sees an IP. |
| 2 | `User.Version` optimistic concurrency or per-column setters? | **`Version`** | One concept instead of seven interface methods. Protects fields added later (T502's `DisabledAt`, T107's `PendingEmail`) without further interface churn, and fails loudly rather than silently losing a write. |
| 3 | `store/sql` separate module or same module? | **Separate nested module** | SQL drivers must not enter the dependency graph of someone using their own store. Preserves the no-new-dependencies rule for the core. |
| 4 | Should `New` return an error? | **Yes** | It now validates config (nil `SecondFactorChecker`, bad Argon2 params). A panic in a constructor is worse, and returning an error matches `totp.NewService` and `passkey.NewService`. |

### Consequences to hold across sessions

- `New` gains a required 4th argument before `opts`. Applications with no second factor pass `sulis.NoSecondFactors{}` — an explicit, greppable declaration rather than a default.
- Every store implementing `UserStore` must honour the version precondition. `storetest.RunUserStore` (T401, landed) enforces it, along with storage-layer email uniqueness on both `CreateUser` and `UpdateUser`.
- `RequestInfo` is threaded through `Login`, `VerifyPassword`, `ChangePassword`, `CreatePasswordResetToken`, `CreateMagicLinkToken`, `RedeemMagicLink`, `CompleteTwoFactor`, and `ReAuthenticate`. Zero value is valid, so tests stay short.
- Raw session tokens are returned beside the `*Session`, never on it (T104).

### Known deferrals

None outstanding. (The NFKC dependency question was resolved on 2026-08-18 — see the Decisions table.)

---

## Decisions

Append as work proceeds. Each entry: task, decision, one-line reason. Mark anything that contradicts `PLAN.md` Appendix A.

| Task | Decision | Reason |
|---|---|---|
| — | Commits use no trailers | User preference, 2026-08-17 |
| — | Plan split into `PLAN.md` (stable) + `PROGRESS.md` (state) | Sessions restart often; one state file avoids divergence |
| T505 | **`golang.org/x/text` approved** — user authorised it 2026-08-18. Amends the Global Constraints in `PLAN.md` | NFKC normalization is needed so a password typed with different Unicode composition still verifies. Hand-rolling it would be worse than depending on the semi-official module that implements the spec. Do **not** treat this as a general relaxation: it is a one-module carve-out, and everything else still obeys the no-new-dependencies rule |
| — | Work on branch `security-hardening-v1`, not `main` | Long breaking-change series; keeps `main` releasable |
| — | `git config user.name/email` set repo-locally to match existing history | No identity was configured; used `Raúl Santos <4837+borfast@users.noreply.github.com>` from prior commits |
| T001 | Actions upgraded v4/v5 → v7, SHA-pinned | Pinning stale majors trades one risk for another; Dependabot keeps them current |
| T001 | Analyzers run in a separate `analyze` job, not the test matrix | Avoids running them twice per Go version |
| T001 | `decodeHash` widens salt/hash lengths *after* the bounds check | Real fix for gosec G115: the conversion is now provably in range |
| T001 | Added `counterAt` epoch guard in `totp` | Real fix: `Generate` is public and takes an arbitrary time; pre-1970 times wrapped `uint64` |
| T001 | 3 `#nosec` suppressions, each with an inline reason | G115 ×2 (bounds enforced 3 lines above, invisible to gosec), G101 (purpose enum, not a credential), G505 (HMAC-SHA1 is required by RFC 6238 and unaffected by SHA-1 collisions) |
| T001 | `GO-2026-5932` (x/crypto/openpgp) accepted, not actioned | Unreachable — sulis imports only `x/crypto/argon2`; no fix exists upstream |
| T101 | `setPassword` signature changed to `(ctx, userID, newPassword string, guard func(*User) error)` | Extends Appendix A (internal, so not a public break). The guard re-runs on each retry, so `ChangePassword`'s old-password check and `SetInitialPassword`'s passwordless check hold against current state, not the caller's first read |
| T101 | `ChangePassword` re-verifies the old password inside the update | A concurrent change must not be overwritten on the strength of a stale check. Costs an extra Argon2 run only on an actual conflict |
| T101 | `stampEmailVerified` reads `hadPassword` from the reloaded row | A password set between the caller's read and this write still triggers the session revocation it is there to guarantee |
| T102 | **Deviates from PLAN.md T102:** the checker is wired into `Login` only, NOT `IssueSession` | `IssueSession` is the post-all-factors primitive that `CompleteTwoFactor` calls. Consulting the checker there would demand a second factor again after it was just verified — an infinite loop. T305 replaces its `userID` argument with an `Authentication` proof, which is the real guard |
| T102 | `RequestInfo` threaded in this task rather than T106 | Otherwise T106 re-touches the same ~35 call sites for no benefit. The parameters are accepted and documented now, and consumed by the limiter in T106 |
| T102 | `New` also validates `MinPasswordLength <= MaxPasswordLength` | Free to add now that it returns an error; an inverted range would otherwise reject every password |
| T102 | `LoginResult.SessionToken` populated from `Session.Token` until T104 | Keeps `LoginResult` correct from the start, so T104 is a pure deletion of the struct field |
| T104 | `CompleteTwoFactor` returns `*LoginResult` and takes `RequestInfo` (Appendix A shape, landed early) | It had to change anyway to surface the raw token; returning `LoginResult` avoids a fourth return value and a second churn |
| T104 | `Register` returns `(*User, *Session, string, error)` rather than a `LoginResult` | Registration is exempt from the second-factor gate by design, so a result type implying a 2FA branch would mislead |
| T106 | Per-prefix budgets with longest-prefix matching, rather than one budget for all keys | Per-account must stay generous (an attacker must not lock a victim out) while per-IP stays tight; one budget cannot serve both |
| T106 | `MemoryLimiter` caps tracked keys (default 100k) and evicts full buckets first | A limiter that can be driven out of memory is a denial of service, not a defence. Full buckets carry no state, so they are free to drop |
| T105 | **Scope split from PLAN.md T105:** the test asserts UV is *requested and recorded in session data*, not end-to-end rejection of a UV-absent assertion. **DISCHARGED by T206** | go-webauthn's enforcement is driven entirely by `session.UserVerification == VerificationRequired`, so that is the library behaviour we control. End-to-end rejection needed a forging test authenticator, which T206 built (`passkey/testauth_test.go`): `TestFinishLoginRejectsAssertionWithoutUserVerification`, `TestFinishDiscoverableLoginRejectsAssertionWithoutUserVerification`, and `TestFinishRegistrationRejectsResponseWithoutUserVerification` each forge a UP-set/UV-clear response and require the rejection, each paired with a `WithUserVerification(VerificationDiscouraged)` control that must accept the identical bytes |
| T107 | `ChangeEmail`'s uniqueness check runs once before staging; `ConfirmEmailChange`'s re-runs inside the `updateUserWithRetry` closure, against the freshly loaded row, on every attempt | Staging doesn't make an address live, so a race there is harmless. Confirmation is where the address actually becomes live, so that check must hold against current state, not the state read before the retry loop started — matching `updateUserWithRetry`'s existing re-establish-invariants contract |
| T107 | `stampEmailVerified` (idempotent, no-op if already verified) is not reused by `ConfirmEmailChange` | Confirmation must re-stamp unconditionally with a fresh timestamp even though `EmailVerifiedAt` was already non-nil for the old address — idempotent semantics would make it a no-op and leave the old address's stamp in place for the new one |
| T107 (fix round 1) | `UserStore` GoDoc now states email uniqueness MUST be enforced at the storage layer, with `CreateUser`/`UpdateUser` returning `ErrUserAlreadyExists` on violation | `Version` only guards a lost update on a single row; two different rows racing to claim the same address (e.g. two accounts both confirming a change to the same staged address) can't be prevented above the store interface. `ConfirmEmailChange`'s own `GetUserByEmail` re-check is a best-effort early rejection, not the guarantee — found in code review |
| T202 | `User.Credentials` removed from the exported struct; a new unexported `webauthnUser` (wrapping `*User` plus a store-loaded `[]Credential`) implements `webauthn.User` instead | `User` is now caller-supplied identity only, so a caller can no longer smuggle in a stale or fabricated credential list — every ceremony (`BeginRegistration`, `FinishRegistration`, `BeginLogin`, `FinishLogin`, `FinishDiscoverableLogin`) builds its own `webauthnUser` from what the store returns. Breaking change to the passkey API surface, called out explicitly since Appendix A didn't anticipate this field's removal |
| T201 | **Extends `PLAN.md` Appendix A** (which names only the `ChallengeStore` method change): `FinishLogin`'s signature changes from `(ctx, user, r)` to `(ctx, user, ceremonyID, r)`, matching `BeginLogin`'s new `(*protocol.CredentialAssertion, string, error)` return | Login challenges move from a per-user key to a per-ceremony key so a second login ceremony for the same user can't clobber the first device's in-flight challenge — the same reasoning `BeginDiscoverableLogin`/`FinishDiscoverableLogin` already used. The ceremony ID has to round-trip through the caller, so `FinishLogin` must accept it. Registration keying stays per-user; the plan only calls for this on login |
| T203 | `Discoverable` is populated from the client's `credProps.rk` extension output — go-webauthn v0.17.4's finished `Credential` (`Authenticator`/`Flags`: UserPresent, UserVerified, BackupEligible, BackupState) exposes no resident-key bit at all, so credProps is the only available signal, not a fallback among several | Confirmed by reading the pinned library source (`webauthn@v0.17.4/webauthn/credential.go`, `registration.go`): `CreateCredential`/`NewCredential` never populate anything from `ParsedCredentialCreationData.ClientExtensionResults`. `Service.FinishRegistration` now parses the response itself (`protocol.ParseCredentialCreationResponse` + `s.wa.CreateCredential`) instead of calling `s.wa.FinishRegistration`, purely to reach `ClientExtensionResults`, which that helper discards |
| T203 | **Adds to `PLAN.md`'s step list:** `BeginRegistration` now also requests the `credProps` extension (`webauthn.WithExtensions(protocol.AuthenticationExtensions{"credProps": true})`), unconditionally (regardless of the configured `ResidentKey` requirement) | `Discoverable` can only ever be populated from `credProps.rk` (see row above), and a browser has no reason to include that extension's output unless the relying party asked for it — without this, `Discoverable` would be `false` for every real client, silently defeating the feature. Mutation-tested: removing the `WithExtensions` call fails `TestBeginRegistrationRequestsCredPropsExtension` |
| T203 | `WithResidentKey`'s default sets both `AuthenticatorSelection.ResidentKey` and the legacy `RequireResidentKey *bool`, following go-webauthn's own `WithResidentKeyRequirement` registration option (`RequireResidentKey` true only for the `Required` case, false otherwise) | Matches the plan's instruction to set `RequireResidentKey` for older authenticators, and mirrors the library's own precedent rather than inventing a new rule |
| T204 | Parsing entry points: `protocol.ParseCredentialCreationResponseBytes`/`ParseCredentialRequestResponseBytes` (both exist in go-webauthn v0.17.4 alongside the `*http.Request` and `io.Reader` variants); session-data finish calls are `wa.CreateCredential` (already in place from T203), `wa.ValidateLogin`, and `wa.ValidateDiscoverableLogin` | Confirmed by reading `webauthn@v0.17.4/webauthn/{registration,login}.go` — these are the byte/session-data-based counterparts to `FinishRegistration`/`FinishLogin`/`FinishDiscoverableLogin`, which all take `*http.Request` directly and so can't be reused from the `[]byte` core |
| T204 | The body-size cap (`checkCeremonyBodySize`) runs as the very first step of each `[]byte` core method — before `ConsumeChallenge` and before any store call — rather than after | "Reject up front" per the task brief; also means a garbage oversized body doesn't burn the caller's still-valid challenge, so a retry with a correctly sized request against the same ceremony still works. Regression-tested (`TestFinishRegistrationRejectsOversizedBody` asserts the challenge saved by `BeginRegistration` is still present after a rejected oversized `FinishRegistration` call) |
| T204 | Two independent enforcement layers, not one: `http.MaxBytesReader` in `http.go`'s `readCeremonyBody` caps what's actually read off the wire; `checkCeremonyBodySize` in `passkey.go` re-checks `len(body)` in the `[]byte` core | The core check alone can't stop an unbounded read (the bytes are already in memory by the time it runs) — that's what `MaxBytesReader` is for. The core check exists so a caller that never uses `net/http` (or supplies a `[]byte` some other way) is bounded too. Mutation-tested: removing `MaxBytesReader` (leaving a plain `io.ReadAll(r.Body)`) is *not* caught by a test that merely POSTs an already-in-memory oversized `[]byte` body (`TestFinishRegistrationRejectsOversizedBody` still passed, because the core check still catches it after the fact) — this gap led to writing `TestFinishRegistrationCapsBodyReadRatherThanReadingItAll`, which sources the request body from a reader backed by 10× the cap and asserts on the number of bytes actually pulled through it (not just on the returned error); that test does fail when `MaxBytesReader` is removed (655360 bytes read instead of ≤65537) |
| T204 | `readCeremonyBody` treats a nil `*http.Request`/`r.Body` as an empty `[]byte(nil)` body rather than returning its own error | Preserves the pre-existing ordering the tests already depend on (challenge consumption happens before body content matters) instead of special-casing nil ahead of it; an empty body still fails later at JSON-parsing if a real ceremony ever reaches that point |
| T205 | **Superseded by the T205 fix-round-1 row below.** Last-factor deletion guard: new `Service.DeleteCredential(ctx, userID, id string, opts DeleteOptions) error` with `DeleteOptions{AllowLast bool}`, exactly as the brief's example — not a bare `force bool` parameter and not a separate `DeleteLastCredential` method | An options struct reads clearer at call sites (`DeleteOptions{AllowLast: true}` vs. a bare `true`) and leaves room for a future guard-related flag without another signature break. A separate method would mean two entry points enforcing the same invariant, which is more surface for the two to drift. `Store.DeleteCredential`/`DeleteCredentialsByUserID` themselves apply no guard — only `Service.DeleteCredential` does, after loading the user's credential list to both confirm ownership of `id` (returns `ErrPasskeyNotFound` if it isn't in that list) and count how many remain. **This shape had a TOCTOU race** (get-count-then-delete, no atomicity) caught in task review — see the fix-round-1 row for the corrected design |
| T205 | `Store.UpdateCredentialSignCount(ctx, credentialID, signCount)` replaced by `UpdateCredentialAfterLogin(ctx, credentialID, signCount, backupState, lastUsedAt)` rather than adding a second store method alongside it | go-webauthn's own storage guidance (`webauthn` package doc, "Storage" section) says sign count, clone-warning, and `BackupState` (when `BackupEligible`) MUST be written back on every successful login so the next ceremony observes current values; `LastUsedAt` is new bookkeeping needed at exactly the same point. Bundling all three in one store call keeps that invariant enforceable in one place instead of splitting it across calls a caller could apply out of order or only partially. Breaking change to `Store`, allowed per the task's binding constraints |
| T205 | `DeleteCredentialsByUserID` and `RenameCredential` have no `Service`-level wrapper; callers use them directly on their `passkey.Store` | Neither needs cross-cutting business logic the way the last-credential guard does (`RenameCredential` is pure caller-supplied metadata; `DeleteCredentialsByUserID` is meant for whole-account deletion, which the brief says must *not* carry the last-credential guard). Matches the plan's own framing of these as direct `Store` additions, not new `Service` API surface |
| T205 | `toWebAuthnCreds` now also sets `Flags.BackupEligible`/`Flags.BackupState`/`Transport` on the `webauthn.Credential` it builds, not just `ID`/`PublicKey`/`AttestationType`/`Authenticator` | Found while implementing: go-webauthn's `validateLogin` (invoked by both `wa.ValidateLogin` and `wa.ValidateDiscoverableLogin`) compares the credential's `Flags.BackupEligible` bit — read from `user.WebAuthnCredentials()`, i.e. `toWebAuthnCreds`'s output — against the fresh assertion's bit, failing the login with "Backup Eligible flag inconsistency detected" on any mismatch. Persisting `BackupEligible` from registration without also feeding it back through `toWebAuthnCreds` would have made every subsequent login fail for a genuinely backup-eligible credential, since the zero-value `Flags` it produced before this change could never match a real `true`. Regression-tested at the `toWebAuthnCreds` unit level (`TestToWebAuthnCredsPreservesBackupFlagsAndTransport`); the full `ValidateLogin`-path test needed a signed assertion vector and was left to T206. **DISCHARGED by T206**: `TestLoginSucceedsForBackupEligibleCredential` forges a BE/BS-set assertion against a backup-eligible stored credential and requires the login to succeed, paired with a contradicting-flags sub-test that must be rejected. Mutation-tested: dropping `Flags` from `toWebAuthnCreds` again fails it with exactly the predicted "Backup Eligible flag inconsistency detected during login validation" |
| T205 | `Credential.LastUsedAt` is `*time.Time` (nil until first post-registration login), matching the existing `EmailVerifiedAt *time.Time` convention in `user.go` rather than `CreatedAt`'s plain `time.Time` | `CreatedAt` always has a value the moment the struct exists; `LastUsedAt` genuinely has a "never happened yet" state (a freshly registered credential that hasn't completed a login), which is exactly what the nullable-timestamp convention elsewhere in the codebase is for |
| T205 | Registration's spec-vector test fixture (`registrationSpecVectorNoneES256`) gained a `transports []string` parameter alongside the existing `credPropsRK *bool`, injected into `response.transports` | Same pattern already established for `credProps`: both are client-reported values layered onto the same signed W3C "none ES256" vector independently of each other. Decoding the vector's own authenticator data (via a one-off script, not committed) confirmed its signed flags already have both BE and BS set, so `BackupEligible`/`BackupState` needed no synthesized addition the way `Discoverable` did — they're a verified property of the fixture as-is |
| T205 (fix round 1) | **CRITICAL finding from task review, fixed: the last-credential guard moved from `Service` into `Store.DeleteCredential`, whose signature changed to `DeleteCredential(ctx, userID, id string, allowLast bool) error`.** `Service.DeleteCredential` is now a thin delegator (`return s.store.DeleteCredential(ctx, userID, id, opts.AllowLast)`); the membership check, the remaining-count check, and the removal are documented as one required atomic operation on the `Store` interface (reference implementations given: SQL `SELECT ... FOR UPDATE` + `DELETE` in one transaction, or a single guarded `DELETE` statement; a mutex-guarded in-memory store does it under one lock) | Reviewer found the original `Service`-level "load credentials, check count, then call `Store.DeleteCredential(ctx, id)`" was a TOCTOU race: two concurrent calls deleting a user's last two credentials (one ID each) could each read count==2 before either delete landed, both pass the guard with `AllowLast:false`, and both succeed — zero credentials left, the exact lockout the guard exists to prevent, reached through the guarded path. Picked the reviewer's preferred fix direction (atomic store operation) over the documented-race fallback, because it matches this series' own established pattern: `ChallengeStore.ConsumeChallenge` (T201), `TokenStore.ConsumeToken`, and `recovery.Store.ConsumeCode` all close this exact race shape by making the check-and-mutate a single atomic contract rather than documenting it as a known gap. A documented-but-real race in a *deletion* guard is worse than in a challenge-consumption guard: the failure mode here is a permanent account lockout, not a retryable ceremony. `fakeStore.DeleteCredential` reimplemented under its (new) `sync.Mutex` to hold the lock across the whole check-and-delete; new `TestDeleteCredentialGuardIsAtomicUnderConcurrentDeletes` (modeled on T201's `TestFinishDiscoverableLoginConsumesChallengeExactlyOnce`) runs 200 iterations of two goroutines racing to delete a user's last two credentials from a shared start gate, asserting exactly one success and one `ErrLastCredential`, and that exactly one credential remains. Mutation-tested: reintroducing the TOCTOU gap in `fakeStore.DeleteCredential` (splitting the check and the mutation into two separately-locked sections) reproduced the predicted failure at iteration 16 (`2 succeeded, 0 rejected`) under `-race`; reverted after confirming. The four pre-existing `Service.DeleteCredential` tests were updated from call-count assertions (`store.deleteCredentialCalls != 0, want 0`) to outcome assertions (`store.deletedCredentialID`, and remaining-credential counts via `GetCredentialsByUserID`), since the store is now always invoked (it may just refuse) |
| T206 | The forging authenticator encodes CBOR through `github.com/go-webauthn/webauthn/protocol/webauthncbor`, **not** `github.com/fxamacker/cbor/v2` directly | Both end up in the same encoder, but `webauthncbor` is a subpackage of go-webauthn — already a *direct* dependency — whereas importing fxamacker directly would promote it from `// indirect` to direct in `go.mod`, i.e. a real (if cosmetic) change to the declared dependency graph, which the task's constraints forbid. Confirmed by running `go mod tidy` after the change: `go.mod` and `go.sum` are byte-identical. `webauthncbor` also uses CTAP2 canonical encoding, which is what a real authenticator emits |
| T206 | Rejection tests for the three *authenticator-data* checks (UV, UP, RP ID hash) are each paired with a control sub-test that replays the identical forged bytes against a Service that must accept them, rather than asserting on the error text | go-webauthn's `protocol.Error.Error()` returns only `Details`, and all three checks share the same `Details` string ("Error validating the authenticator response"); the distinguishing text is in `DevInfo`, which sulis does not surface (both `FinishLoginResponse` and `FinishRegistrationResponse` wrap with `%v`, not `%w`, so the `*protocol.Error` isn't in the chain either). Without a control, such a test would pass for any rejection whatsoever — decoration. The client-data checks (challenge, origin, ceremony type) and the signature check *do* have distinct `Details`, so those tests assert on the message directly |
| T206 | `TestFinishLoginConsumesChallengeExactlyOnce` uses 8 racers, not the 2 T201's discoverable-login version uses | `FinishLoginResponse` loads the user's credentials *before* consuming the challenge, and that store call staggers the goroutines' arrival at the consume step; with 2 racers the window is missed most runs. It also asserts the stronger property T201's version could not: because the forged assertion is genuinely valid, the expectation is exactly 1 success and 7 `ErrChallengeExpired`, so a leaked challenge shows up as a *successful login replayed against one ceremony* rather than merely as a second non-expired error |
| T206 | `fakeStore.SaveCredential` now really persists (into both lookup maps), and `UpdateCredentialAfterLogin` keeps the by-user list in step with the by-ID index | A registration ceremony followed by a login ceremony against the credential it just created is the round trip a real deployment performs, and it is the only thing that proves the two halves agree on what was stored — the old no-op `SaveCredential` made that untestable. Keeping the two indexes in step matters for clone detection specifically: a second login must read the *advanced* sign count, which is what a real store would show it |
| T301 | No new sentinel needed — `ErrTOTPInvalid` already existed in `totp/totp.go` (added at T001) but was unused by `Validate`, which returned `(false, nil)` for a wrong code instead | Reused the existing sentinel rather than adding another one. `ConfirmEnrollment` already returned it for the same "wrong code" condition, so `Validate` returning it too makes the two call sites consistent |
| T302 | Store-contract shape for "separates pending from active" (Appendix A): `Store` gains `GetActiveTOTP`/`GetPendingTOTP` (one slot each, `ErrTOTPNotEnrolled` if empty), `EnrollPending`/`ReplacePending` (create-or-replace the pending slot; only `EnrollPending` checks for an existing active credential), and `ConfirmEnrollment(ctx, userID, pendingID string, counter uint64) (*Credential, error)` (atomic promotion, compare-and-swap on `pendingID`). `GetTOTPByUserID` and the old unconditional `SaveTOTP`-based `Enroll`/`ConfirmEnrollment` are removed; `SaveTOTP` narrows to persisting updates on an already-active credential (in practice, only `Validate`'s counter bump calls it now) | Considered a single `SaveTOTP(cred, requireNoActive bool)` boolean-flag shape (mirroring `DeleteCredential`'s `allowLast bool`) instead of two named methods (`EnrollPending`/`ReplacePending`), but two names read clearer at the call site and map 1:1 onto `Service.Enroll` vs `Service.ReplaceEnrollment` — the boolean would just re-derive that same fork inside the store. Reusing the name `ConfirmEnrollment` on both `Service` and `Store` mirrors the house style already set by `passkey`'s `DeleteCredential` (same name, `Service` is the thin/atomicity-adjacent wrapper, `Store` holds the actual guarantee) |
| T302 | **Atomicity lives in the store, on `Store.ConfirmEnrollment`'s `pendingID` compare-and-swap — not in `Service`.** `Service.ConfirmEnrollment` still does a non-atomic `GetPendingTOTP` (to validate the submitted code against the pending secret) before calling the store's atomic promotion, passing the pending credential's own ID forward as `pendingID`; the store's contract requires it to reject the promotion (`ErrTOTPNotEnrolled`) if the live pending enrollment's ID no longer matches, i.e. a racing `EnrollPending`/`ReplacePending` superseded it in the gap. This is the same shape as `T101`'s `User.Version` check and `DeleteCredential`'s membership+count+removal atomicity: the read that needs external computation (matching the code) stays outside the lock; the compare-and-swap that must not tear closes the gap | A fully store-side "validate the code too" design was rejected: the store has no reason to know about HMAC/TOTP code matching, and pushing it there would leak a `totp`-specific algorithm into every store implementation instead of keeping `Store` a plain persistence contract, matching how `ConsumeToken`/`ConsumeChallenge` also only compare opaque identifiers (hash, key), never re-derive the thing they're consuming |
| T303 | Zero-rows layering: `SessionStore.DeleteSession(ctx, userID, id)` itself returns `ErrSessionNotFound` when the scoped delete matches nothing — whether `id` doesn't exist or belongs to a different user — rather than `Service.RevokeSession` doing a separate ownership read before calling a `DeleteSession(ctx, id)` that stays unscoped | Matches this series' existing house style for a check-and-mutate store contract (`passkey.Store.DeleteCredential`'s membership+count+removal, `ConsumeToken`, `ConsumeChallenge`, `ConsumeCode`): the check and the mutation are one atomic store-side operation, not two calls a caller could split. A `Service`-level pre-check (`GetSession` then compare `UserID` then `DeleteSession`) would reintroduce exactly the TOCTOU shape T205's fix-round-1 closed — the session could be deleted or expire between the read and the delete, and the ownership compare would run against data that's no longer current. `RevokeSession` itself stays a one-line delegator |
| T302 | `ConfirmEnrollment` is no longer idempotently re-callable on an *already-promoted* pending enrollment (a client retry of an already-succeeded confirm now gets `ErrTOTPNotEnrolled`, not a silent no-op re-save) — a deliberate behavior change from pre-T302, which allowed exactly this (old `ConfirmEnrollment` just re-matched the code against whatever `GetTOTPByUserID` returned, verified or not) | Once a pending enrollment is promoted, its slot is consumed exactly once — mirroring the single-use contract `ConsumeToken`/`ConsumeChallenge` already place on their own resources — rather than staying re-confirmable indefinitely. The old `TestConfirmEnrollmentDoesNotRollBackCounter` exercised re-confirming the *same* pending enrollment twice, which this removes; its replacement, `TestConfirmEnrollmentCarriesCounterForwardAcrossReplace`, pins the equivalent (and, we think, actually-intended) property under the new split: the counter carry-forward guarantee applies across a **factor replacement** (`ReplaceEnrollment` + `ConfirmEnrollment`), not a repeated confirm of one enrollment. Functionally this is not a security regression — the account is already 2FA-protected after the first successful confirm — only a UX/robustness change worth flagging for whoever builds the HTTP layer in a later phase: a confirm-retry should be handled by the caller checking current enrollment status, not by resubmitting the code |
| T304 | The unknown-address equalization work lives behind a new package-level `var resetTokenGenerator = generateRawToken` in `sulis.go`, called only from the discarded branch, rather than routing the known-address path through it too | `createTokenForUserWithEmail` (which the known-address path already uses) is shared by email verification, email change, 2FA, and magic link, so rerouting its internal `generateRawToken` call through a "reset token" seam would mislabel it and widen the blast radius for no benefit — this task's file list is `sulis.go`/`sulis_test.go` only. The var exists solely so a test can wrap it with a counting closure and prove the unknown-address path actually burns generation+hashing work, without needing a store write to observe it (store observation alone can't prove *work happened*, only that nothing was *persisted*) |
| T304 | `CreatePasswordResetToken`/`CreatePasswordResetTokenStrict` share one unexported `createPasswordResetToken(ctx, email, ri, strict bool)`, mirroring the `Service`-level thin-delegator pattern already used elsewhere in this series (e.g. `passkey.Service.DeleteCredential`) | One implementation for the shared normalize→limiter→lookup skeleton, with `strict` selecting only the one branch (return `ErrUserNotFound` vs. burn-and-discard) that differs between the public and admin-tooling entry points |
| T305 | Zero-value rejection is a sentinel, `ErrNotAuthenticated`, checked as `auth.userID == ""` | An empty user ID can't be a real proof (the constructor always sets one), so it's a reliable stand-in for "is this the zero value" without needing a `reflect.DeepEqual`/comparable-struct check. A sentinel (rather than a bespoke message per call site) matches the house style already set by `ErrEmailNotVerified`/`ErrRateLimited`/etc., and reads clearly in an `errors.Is`/`==` check |
| T305 | `Authentication`'s unexported constructor, `newAuthentication(userID string, method AuthMethod) Authentication`, is genuinely called by production code — `IssueSessionUnchecked` mints one internally and hands it to a shared `issueSession(ctx, auth)` helper that `IssueSession` also calls after its zero-value check | Considered leaving `newAuthentication` as a test-only helper (constructed directly via struct literal in test code, which is legal since tests are in-package), but routing `IssueSessionUnchecked` through it too means the two exported entry points share one implementation instead of `IssueSessionUnchecked` duplicating `IssueSession`'s load-gate-create body, and it means the constructor isn't dead code outside of tests. Verified with `staticcheck`: no unused-symbol finding either way, but the shared-implementation shape was preferred for the DRY reason above |
| T305 | `IssueSession`/`IssueSessionUnchecked`/`issueSessionForUser`/`Authentication` moved from `sulis.go` into `issue.go` | Matches `PLAN.md`'s File Map, which already describes `issue.go` as "session issuance + `Authentication`" (written before this task, at the plan's inception) — `createSession` and `completeFirstFactor` already lived there since T102 |
| T305 | `completeFirstFactor`'s session branch and `CompleteTwoFactor` keep calling `createSession`/`issueSessionForUser` directly rather than minting an `Authentication` and routing through `IssueSession` — the brief's explicitly offered alternative | Both call sites already hold the `*User` in hand (from their own `GetUserByID`/store reads); routing through `IssueSession(auth)` would force a second, redundant `GetUserByID` by ID for every login and every completed 2FA — exactly the round trip `issueSessionForUser`'s own doc comment says it exists to avoid for `Login`. The brief's "done when" criterion is about the PUBLIC surface (no exported way to mint a session from a bare user ID without saying `Unchecked`), which holds regardless of how these two internal, already-authenticated call sites reach `createSession`. Added a short comment at the `CompleteTwoFactor` call site recording this reasoning, since a future reader might otherwise "fix" it into a redundant lookup |
| T305 | `IssueSessionUnchecked` takes `method AuthMethod` (per Appendix A) but does not yet persist it anywhere — `Session` has no method field | Appendix A specifies the parameter; keeping it, even though nothing currently reads it, keeps `IssueSession`/`IssueSessionUnchecked` symmetric (both know which credential authenticated the session) and gives a future consumer (e.g. T509's security event sink) something to read without another signature break |
| T401 | **One `storetest` package and one `memstore` package, both at the module root**, each covering all seven interfaces — not `passkey/storetest`, `totp/memstore`, etc. | Nothing in the library imports either package, so a root-level package that imports `sulis`, `passkey`, `totp`, and `recovery` creates no cycle, and an adopter gets one import line instead of four. Per-subpackage placement would have bought nothing: it does not avoid the cycle described in the row below, which is caused by the direction of the dependency, not its depth |
| T401 | **The existing in-memory test doubles stay in their `_test.go` files rather than moving to `memstore`** — contrary to the task brief's step 4 | Go forbids it. Every test file in this module is an *internal* test (`package sulis`, `package passkey`, `package totp`, `package recovery`) because they all exercise unexported behavior, and `memstore` imports those same packages, so importing `memstore` from any of them is rejected outright: `import cycle not allowed in test` (verified empirically, not assumed — probe file added and removed). The only workaround is converting ~4,500 lines of internal tests to external `_test` packages, which would mean qualifying every unexported reference and losing the coverage of unexported behavior those tests exist for — a direct violation of this task's "do not weaken any existing test" constraint. Each surviving double now carries a comment naming `memstore` as the canonical implementation and stating why it cannot be imported. See the Deferred row for the drift risk this leaves |
| T401 | Instrumented doubles (`observingSessionStore`, `sharedSessionStore`, `errTokenStore`, `failUpdateUserStore`, `raceCreateUserStore`, `wrappedNotFoundUserStore`, `failSessionStore`, `failGetUserStore`, `passkey.fakeStore`'s counters and injectable errors, `totp.failOnNthSaveStore`, `confirmRaceStore`) stay in the test files unconditionally, and `memstore` carries no test hooks (no `beforeUpdate`, no injectable failures, no call counters) | The brief left this open. `memstore` is public API and a reference implementation: an adopter reads it to learn where the atomic boundary goes, and instrumentation would be noise in exactly the code that has to be exemplary. Test-only affordances belong with the tests that need them. The one exception is `SessionStore.Len`, which reports the stored-session count — not an interface method, documented as such, and there because "this flow created no session at all" is otherwise unobservable through `SessionStore` |
| T401 | `storetest`'s concurrency subtests repeat 100 times by default and 5 under `-short`, and call the factory once per iteration | A race that loses once proves nothing, so a single pass would make the suite decoration. 100 is enough that every deliberately broken store built for this task failed at iteration 0 or 1. `-short` exists because an adopter running against a real database pays a schema reset per iteration, and smoke-testing is a different job from certifying |
| T401 | `storetest` asserts two things the interfaces do not spell out: values returned by a store must not alias its internal state, and `SaveChallenge` must not retain the caller's slice | Both are trivially true of any database-backed store and can only fail for an in-memory one with an aliasing bug — precisely what the suite should catch. The alternative is a store where `GetUserByID` hands back a pointer a caller can mutate the row through, which would defeat `Version` entirely. Deliberately *not* asserted, for the opposite reason: timestamp precision and location, `Metadata` **value** round-tripping, and any ordering the interfaces do not promise — a SQL store legitimately differs on all of them. **Amended in fix round 1:** the no-aliasing half is no longer "beyond the GoDoc" — it is now stated on `UserStore` and `SessionStore` themselves (see the row below) |
| T401 (fix round 1) | **`UserStore` and `SessionStore` GoDoc now state that a store MUST NOT share mutable state with its callers in either direction**, naming `Metadata`'s map and `EmailVerifiedAt`'s pointer as the two traps; `storetest`'s two isolation subtests were extended to populate and mutate both, on all three boundary crossings (`CreateUser`, `UpdateUser`, `Get*`); `memstore` now deep-copies via `maps.Clone` plus a pointed-to-time copy; the root package's `memUserStore`/`memSessionStore` doubles got the same treatment so they are not weaker than the contract | Review found `memstore` doing shallow `cp := *user` copies on types carrying a map, which contradicted `memstore`'s own package doc ("values returned to callers are always copies… can never reach inside the store") in the one package adopters are told to read as exemplary. A map copy is a reference copy, so the bug is not cosmetic: it hands any caller a live handle on a persisted row and lets them rewrite it without going through `UpdateUser`, which does not violate the `Version` precondition so much as walk around it. Since every conforming store has to get this right and only in-memory ones can get it wrong, it belongs in the contract and the suite rather than in one implementation |
| T401 (fix round 1) | Maps are cloned **one level deep**; a caller that stores a map or slice as a `Metadata` *value* still shares that inner value with the store, and this is documented rather than fixed | `Metadata` is `map[string]any` and sulis never reads it, so nothing in the library knows how to copy an arbitrary `any` — a recursive clone would need reflection, would silently change types it could not reproduce, and would be copying data the library has no stake in. One level is where the store's own invariants stop being at risk: the map the store holds is its own, and what a caller puts inside it is the caller's business. Stated in `cloneUser`'s doc comment, `memstore`'s package doc, the `UserStore`/`SessionStore` GoDoc, and the README |
| T401 (fix round 1) | `storetest/passkey.go` imports `github.com/go-webauthn/webauthn/protocol` — so "storetest depends only on stdlib + the sulis module itself" (brief constraint) is not literally true | Not a new dependency: it is already required by `go.mod` and already on `passkey.Store`'s own public surface, since `Credential.Transports` is `[]protocol.AuthenticatorTransport`. Anyone who can implement `passkey.Store` already imports it. The alternative — not asserting that `Transports` round-trips — would drop a real requirement, since go-webauthn re-checks transports and the backup flags on every subsequent ceremony. Recorded here so the dependency note is honest rather than technically-clean |
| T402 | Each of the four fuzz targets uses one `f.Fuzz` input shape that lets a single seed corpus pin two different properties at once, rather than splitting into separate fuzz functions per property | `FuzzDecodeHash` takes one `string` and both feeds it straight to `decodeHash` (no-panic, on the raw fuzzer-chosen bytes) and hashes it with `testArgon2Params` before decoding the result back out (round-trip). `FuzzNormalizeEmail` and `FuzzRecoveryCanonical` each take one `string` and check idempotency by calling the function again on its own output. `FuzzGenerateCode` takes `(secret string, unixSec int64, digitsRaw int)`, clamping `digitsRaw` to 6–8 (`6 + int(uint32(digitsRaw)%3)`) — the range `totp.NewService` enforces — so an out-of-range `Digits` doesn't fail the length property for a reason unrelated to the base32 decoder actually under fuzzing. Native Go fuzzing only permits one `f.Fuzz` call per `testing.F`, so this shape is what let one seed corpus (not two, with the corpus-vs-property mapping to keep straight) exercise both properties named in the brief for `FuzzDecodeHash` |
| T402 | `FuzzGenerateCode` also asserts the T001 `counterAt` epoch guard directly (pre-epoch time ⇒ error, not just "doesn't panic") | The brief named "pre-1970 times" as "a good seed" without specifying what the target should assert about them beyond not crashing. A silently-wrapped counter for a negative Unix time would produce a well-formed-looking code (right length, all digits) that just happens to be wrong — the length/digit-content checks alone can't catch that regression, only an explicit "pre-epoch must error" assertion can. Confirmed load-bearing: temporarily removing `counterAt`'s `secs < 0` guard passed every other property check but failed this one on both pre-epoch seeds |
| T402 | Each fuzz target's properties were shown to bite before the real fuzzing runs, by temporarily reverting the guarantee in production code and running the seed corpus (not the full 60s fuzz loop) as a fast regression check, then reverting | Matches this series' established "mutation-tested" convention (see nearly every prior Decisions/Session-log entry). Four mutations, each confirmed and reverted: `decodeHash`'s `len(parts) != 6` bounds check removed → panics on the empty-string seed (`index out of range [1]`); `hashPassword`'s salt/hash `Sprintf` arguments swapped → every round-trip seed fails on the length mismatch; `normalizeEmail`'s `strings.ToLower` call dropped → the mixed-case seed fails the lowercase check; `generateCode`'s digit-count format hardcoded to `"%06d"` → initially every existing seed still passed by coincidence (mod is still computed from `cfg.Digits`, so `%06d`'s zero-padding only under-pads when the actual code value needs strictly more than 6 but fewer than `cfg.Digits` significant digits) — a deterministic RFC 6238 vector known to produce a leading-zero 8-digit code (`"07081804"` at Unix time 1111111109) was added as a permanent seed specifically to make this one reproduce on demand, and does |
| T402 | Fuzzing found one genuine bug, in `recovery.canonical`, within 2 seconds of the first real (non-seed) 60s run: `canonical("-\t0")` (dash, tab, digit) is not idempotent — `canonical("-\t0") == "\t0"` but `canonical(canonical("-\t0")) == "0"` | Root cause: the old implementation was `strings.ToUpper(strings.ReplaceAll(strings.TrimSpace(code), "-", ""))` then a second `ReplaceAll(code, " ", "")` — `TrimSpace` only strips whitespace from the string's *ends*, and only *before* dashes are removed. A tab sitting between a leading dash and the rest of the code isn't at an edge on the first pass (the dash is), survives dash-removal, and becomes the new leading character — so a *second* `canonical` call trims it away, which the first call didn't. Also latent: the second `ReplaceAll` only strips literal ASCII space (`" "`), not tabs or other Unicode whitespace, so interior tabs survived entirely in some shapes. Fixed by replacing the two-step implementation with one left-to-right pass (`strings.Builder` skipping `unicode.IsSpace` runes and `'-'`, uppercasing everything else via `unicode.ToUpper`) — trivially idempotent for any input, since nothing it strips or transforms can reappear or change on a second pass. Confirmed the fix compiles and passes with `unicode` newly imported; confirmed the pre-fix code fails both the fuzz crasher regression seed and a new named unit test, `TestCanonicalIsIdempotentWithInteriorWhitespace` (`recovery/recovery_test.go`), by temporarily reverting to the buggy implementation and observing both fail identically to the original fuzzer output, then re-applying the fix. Not a known-exploitable defect (real codes from `Generate` are always clean lowercase-base32-plus-dashes and never hit this path; `canonical` is also never called twice on the same value in production, only once per `hashCode`), but a real correctness gap in a function whose whole job is stable normalization for hashing, worth closing outright rather than deferring | 
| T402 | `impact()` on `canonical` (upstream) reported LOW risk before the fix (1 direct caller, `hashCode`); `decodeHash`, `normalizeEmail`, and `generateCode` all reported HIGH risk (each has several direct callers spanning `Login`/`ChangePassword`/`CreateMagicLinkToken`/`CreatePasswordResetToken`/`ConfirmEnrollment`/`Validate`), per this project's CLAUDE.md instruction to warn on HIGH/CRITICAL impact before editing | Only `canonical` (LOW risk) was actually edited in production code this task; the other three parsers were fuzzed but never needed a fix, so their HIGH-risk rating never came into play against an actual edit. Flagged here per the project's HIGH-risk warning rule, since the task brief explicitly authorized fixing any real bug fuzzing surfaced in any of the four, and that authorization was exercised for one of them |
| T402 | Checked-in fuzz corpus is the `f.Add` seed values embedded in each `fuzz_test.go`, plus one crasher file (`recovery/testdata/fuzz/FuzzRecoveryCanonical/3853ddfc31ee1eab`) — not a large `testdata/fuzz/**` tree of "interesting" coverage-increasing inputs per target | Go's fuzzing engine only ever writes two kinds of files under `testdata/fuzz/<FuzzName>/`: crashers (always) and inputs a developer explicitly promotes from the local build-cache generated corpus (never automatic). The "new interesting: N" the 60s runs reported lived in the local build cache (`$GOCACHE/fuzz`), not the module tree — confirmed empirically: after all four 60s runs, `testdata/fuzz/**` only contained the one crasher. This is standard Go fuzzing practice, not a shortcut: the `f.Add` seeds are the real "seed corpus" the brief asks for, and are committed in source where they're readable and reviewable, rather than as opaque binary-ish corpus files |
| T501 | `SessionStore` gains a dedicated `UpdateAuthenticatedAt(ctx, id, at)` rather than adding a parameter to (or otherwise folding this into) the `TouchSession(ctx, id, lastSeen, idleExpires)` Appendix A reserves for T503 | The brief flagged this explicitly: pick a shape that won't collide. A step-up re-authentication and a session-liveness "last seen" touch are different events, fired by different callers at very different frequencies (a re-auth prompt vs. every request), and are not always both true at once — a session can be actively used (fresh `lastSeen`) for hours without ever re-proving its password, and `ReAuthenticate` doesn't imply the session was just "seen" in the liveness sense either, if e.g. it's called from a background re-auth check. Folding both into one method's parameters would force every caller of one to pass a value for the other, and a caller that only meant to refresh one would silently refresh both if it reused the same helper carelessly. Two named methods keep the two concerns as separable as they are in reality; `TouchSession` itself is untouched by this task, left for T503 to add exactly as Appendix A specifies |
| T501 | `Authentication.at` (T305's unexported timestamp field, minted by `newAuthentication` but never read by anything until now) is threaded through `issueSessionForUser`/`createSession` and stamped onto the minted `Session.AuthenticatedAt` for the `IssueSession`/`IssueSessionUnchecked` path; `completeFirstFactor` and `CompleteTwoFactor` (which don't construct an `Authentication` at all, per the T305 Decisions row) pass a fresh `time.Now()` at their own call site instead | T305 minted `at` at proof-construction time specifically so a session's authentication stamp could reflect *when the factor was verified*, not *when the session row happened to be written* a store round-trip later — but nothing consumed it, so it sat as dead weight documented only by a doc comment. This task is that consumer: `Authentication`'s field comment is updated to say so, discharging the "why does this field exist and go unread" question a reviewer would otherwise have to re-derive from git history. For `completeFirstFactor`/`CompleteTwoFactor`, there is no `Authentication` in scope to read an `at` from (that's the whole point of T305's decision not to route them through one), so `time.Now()` at the point of session creation is the only timestamp available — functionally identical in practice, since verification and session creation are back-to-back calls with no meaningful gap between them |
| T501 | `createSession(ctx, userID, method, authenticatedAt)` grew two parameters instead of accepting a struct or an `Authentication` | Every call site already has both values close at hand (a literal `AuthMethod` constant or an in-scope `method` parameter, and either `auth.at` or a fresh `time.Now()`), so a wrapper struct would only add an allocation and a name to thread through for no readability gain at four call sites. Accepting `Authentication` itself was rejected because two of the four callers (`Register`, `CompleteTwoFactor`) have no `Authentication` to offer and would have to fabricate one just to satisfy the signature — manufacturing the exact "proof" type T305 built to be unforgeable from outside legitimate flows, inside the one file where doing so is easiest to get subtly wrong |
| T501 | `ReAuthenticate` loads the user via `session.UserID` (`GetUserByID`), then rate-limits on `"password:"+user.Email` — the same order `ChangePassword` uses, not `VerifyPassword`'s order (rate-limit first, using a caller-supplied email) | `ReAuthenticate`'s signature has no email, only a `*Session` (which carries `UserID`, not `Email`) and a password, so the email needed for the rate-limit key can only come from a store read — exactly `ChangePassword`'s situation, and unlike `VerifyPassword`/`Login`, which take the email as an argument and can rate-limit before ever touching a store. Reusing the `"password:"+email` key (rather than a distinct `"reauth:"` prefix) is deliberate: `ReAuthenticate` guessing a password is the same brute-force risk against the same account as `Login`/`VerifyPassword`/`ChangePassword` guessing it, and giving it a separate budget would let an attacker who exhausted one channel keep guessing through another |
| T501 | `ReAuthenticate` mutates the caller's `*Session` in place (`session.AuthenticatedAt = now`) in addition to writing the store, even though Appendix A's signature returns only `error` | The store write is the source of truth and would be sufficient on its own, but the caller already holds the exact `*Session` this call just refreshed, and a caller that immediately calls `RequireRecentAuth(ctx, session, ...)` again in the same request (e.g. after a step-up prompt, before proceeding to the now-authorized operation) would otherwise have to `ValidateSession` again just to see its own successful write reflected. Costs nothing extra: the store call already succeeded before this line runs |
| T501 | Zero-value `AuthenticatedAt` needs no special-case guard in `RequireRecentAuth` | `time.Since` on the zero time (year 1, `time.Time{}`) is on the order of two thousand years in `time.Duration` terms (which saturates rather than overflowing, per the stdlib's documented behavior for inputs this large) — comfortably larger than any `maxAge` an application would configure, so the existing `time.Since(session.AuthenticatedAt) > maxAge` comparison already fails closed on a pre-T501 session with no extra branch. Verified directly: `TestRequireRecentAuthZeroAuthenticatedAtFailsClosed` passes against the plain implementation with no `IsZero()` special case anywhere in `RequireRecentAuth` |
| T502 | The disable reason is a dedicated field, `User.DisabledReason string`, not a `Metadata["disabled_reason"]` entry | `Metadata` is documented (`user.go`, and the README) as caller's-own-business data `sulis` never reads; `reason` is a parameter to a `sulis` method (`DisableUser`) whose semantics `sulis` itself owns and whose contract (`EnableUser` clears it, `storetest` round-trips it) the library is responsible for, which is exactly the boundary `PendingEmail` already draws for the same reason. A `Metadata` entry would also be silently clobberable by anything else writing through that map, and untyped |
| T502 | Failure counting needs no new `UserStore` method — `User.FailedLoginAttempts int` plus the existing `LockedUntil *time.Time` are ordinary fields written through the existing `GetUserByID`/`UpdateUser`/`Version` machinery via `updateUserWithRetry`, the same as `DisabledAt` | This is the concrete case the T002 Ratified API's Decision #2 reasoning named in advance ("protects future fields added later (T502's `DisabledAt`) without further interface churn") — considered and rejected a dedicated `IncrementFailedLoginAttempts`/`ResetFailedLoginAttempts` store method pair, since `Version`'s whole point is that a store implementer never has to add a method just because the root package added a field, and a bespoke atomic-increment method would only buy safety `updateUserWithRetry`'s reload-mutate-retry loop already provides for every other field on `User` |
| T502 | `accountStatus(user *User) error` is a private, pure (no store I/O) helper reused by `completeFirstFactor`, `issueSessionForUser`, and `CompleteTwoFactor`; `VerifyPassword` and `ValidateSession` do **not** call it — they inline the equivalent checks directly | `VerifyPassword` needs the two checks split across the wrong/right-password branches (oracle ordering, see the row below), which `accountStatus`'s single combined check can't express, so reusing it there would need an extra branch anyway. `ValidateSession` checks only `DisabledAt`, never `LockedUntil` (see the next row) — a different check than `accountStatus` performs, not a subset of it, so calling `accountStatus` there and ignoring half its meaning would be misleading at the call site |
| T502 | **`ValidateSession` checks only `DisabledAt`, never `LockedUntil`** | The T502 brief's stated test only requires this for the disabled case ("disable, then validate a pre-existing session → `ErrAccountDisabled`"); the lockout test only concerns fresh authentication ("locked → `ErrAccountLocked`; after the deadline → authentication works"), never session validation. This is also the more defensible behavior on its own: automatic lockout throttles *new* authentication attempts after repeated guessing; a session was only issued because the owner already proved their identity once, and an automatic, attacker-triggerable mechanism (repeated wrong passwords from anyone, not necessarily the session holder) being able to kill that session too would make the denial-of-service risk `WithFailureLockout`'s own doc comment warns about strictly worse, not better. `DisableUser`, by contrast, is an operator's deliberate decision, not an attacker-triggerable side effect of guessing, so killing sessions immediately is exactly the point |
| T502 | **Extends the T502 brief's file list to include `twofactor.go`** (not listed) — `CompleteTwoFactor` gained the same `accountStatus` check as `completeFirstFactor`/`issueSessionForUser` | `CompleteTwoFactor` mints a session directly (`s.createSession(...)`), exactly like the two listed call sites — an account disabled in the window between a verified first factor and second-factor completion (the pending token's lifetime, default 5 minutes) must not be able to finish logging in on a token minted before the disable. The brief's operative instruction is "status checks in ... EVERY issuance path," and this is one; the file list undercounted it |
| T502 | **Does NOT extend to `stepup.go`'s `ReAuthenticate`** — it does not call `accountStatus` and can still refresh `AuthenticatedAt` for a disabled/locked account's already-held session | `stepup.go` is not in the T502 brief's file list, and unlike `CompleteTwoFactor`, `ReAuthenticate` never issues a session — "every issuance path" does not cover it on the same reading that justified the `twofactor.go` extension above. Practical exposure is narrow: `ValidateSession` already blocks a disabled account's session on every subsequent call regardless of whether `ReAuthenticate` quietly succeeded first, so the gap cannot be used to keep the session alive past that check. Recorded in Deferred below rather than silently left out, since a caller reasoning from "every issuance path is gated" could otherwise be surprised |
| T502 | Account-status oracle discipline in `VerifyPassword`: the wrong-password branch calls `recordFailedLogin` unconditionally when lockout is configured (never conditioned on `DisabledAt`/`LockedUntil`), and `accountStatus` is checked only in the branch reached after `verifyPassword` returns `true` | Checking disabled/locked status on the *wrong*-password branch — even just to decide whether to skip `recordFailedLogin` — would mean the internal state read (`user.DisabledAt`) differs by branch in a way that could eventually leak through timing or a future refactor; keeping the wrong-password branch fully uniform (always record, when configured, regardless of status) removes that whole class of mistake at the cost of the failure counter also advancing for the wrong password against an already-disabled account, which is harmless (the account can't authenticate either way while disabled) |
| T502 | `lockoutBackoff(base, max time.Duration, excess int) time.Duration` is a free function, not a method, and is unit-tested directly (`TestLockoutBackoffGrowsExponentiallyAndCaps`) rather than only indirectly through `VerifyPassword` | It has no dependency on `*Sulis` — pure arithmetic — and the exponential-growth/cap/overflow-safety property (excess clamped to `[0,32]`; a zero or negative shifted result also clamps to `max`) is exactly the kind of thing worth pinning with exact input/output pairs rather than reconstructing indirectly from timestamps produced by a real `VerifyPassword` call, which is what the end-to-end `TestWithFailureLockoutLocksAfterThreshold` does for the integration-level property instead |
| T502 | `TestWithFailureLockoutLocksAfterThreshold`'s "lockout window has passed" step uses direct store manipulation (`lockUserUntil`, mirroring `verifyUserEmail`'s existing pattern) rather than `time.Sleep` | `Sulis` has no injectable clock for its own `time.Now()` calls (only `MemoryLimiter` has one, via the unexported `withClock`, and only for its own bucket refill math) — sleeping past a real backoff would work but adds real wall-clock time and a flakiness risk under CI load for no benefit over deterministically rewriting the one field the check reads. `TestAccountLockBlocksLoginUntilDeadlinePasses` uses the same technique to pin the pure locked/expired check in isolation from `WithFailureLockout` entirely, matching the brief's required mutation-test isolation ("remove the lockout expiry check -> recovery test fails") |
| T502 | `gitnexus impact` (upstream, `summaryOnly`) run before editing on `VerifyPassword`, `ValidateSession`, `completeFirstFactor`, `issueSessionForUser`, `CompleteTwoFactor`, and `setPassword`: all LOW risk (0–1 direct dependents) except `setPassword`, **HIGH** (3 direct dependents: `ChangePassword`, `ResetPassword`, `SetInitialPassword`) | Flagged per the project's HIGH-risk warning rule — but `setPassword` itself was never edited this task (lockout bookkeeping is cleared only inside `VerifyPassword`'s own success path, not wired into `setPassword`; see the Deferred row below), so the HIGH rating never came into play against an actual change, the same outcome T402's Decisions row recorded for `canonical`'s upstream-HIGH siblings that were fuzzed but not fixed |
| T502 | `gitnexus detect_changes` (scope `all`) after implementing reported `risk_level: high` (19 changed symbols, 9 files, 13 affected processes) | Verified via `git diff` against every listed symbol: `defaultConfig`, `consumeToken`, and the four `sulis_test.go` `memSessionStore`-related entries are the same line-shift false-positive pattern T303/T304/T305/T402 already documented (none appear anywhere in the actual diff — confirmed with `grep -c` against each file's diff, all zero); the `UserStore` interface and its five methods are flagged because their doc comment (not behavior) now names `DisabledAt`/`LockedUntil` in the aliasing warning, a real but non-functional change. Genuinely-changed, intended: `VerifyPassword`, `completeFirstFactor`, `issueSessionForUser`, `CompleteTwoFactor`, `User`/`User.Version` (struct doc comment). `ValidateSession` itself was NOT listed despite a real, confirmed (`grep -n ErrAccountDisabled sulis.go`) edit — likely the same kind of indexing-lag gap `codegraph`'s own docs describe (index trails a write by ~1s) rather than a missed edit; `status.go`'s five new symbols (`DisableUser`, `EnableUser`, `accountStatus`, `recordFailedLogin`, `clearFailedLogins`, `lockoutBackoff`) don't appear either, expected since it's a new untracked file and plain `git diff` never covers those |
| T502 (fix round 1) | **IMPORTANT finding from task review, fixed: `setPassword` now clears `FailedLoginAttempts`/`LockedUntil` on success, inside its existing `updateUserWithRetry` mutate callback (no extra store round trip); `DisabledAt`/`DisabledReason` are deliberately left untouched.** Covers all three callers (`ChangePassword`, `ResetPassword`, `SetInitialPassword`) with one change, since they all route through `setPassword` | Reviewer found the gap the original Deferred row had only flagged, not fixed: with `WithFailureLockout` enabled, an attacker can lock a victim's account with nothing but repeated wrong guesses, and the victim's own recovery — `ResetPassword`'s out-of-band token, a *stronger* identity proof than a login password — would not lift the lock until `maxBackoff` passed, worsening the exact denial-of-service risk the feature's default-off posture and its "prefer a long backoff" guidance both exist to limit. Clearing inside the existing mutate closure (rather than a separate call after `updateUserWithRetry` returns) means the write is folded into the one store round trip `setPassword` already pays, under the same `Version` compare-and-swap, and re-runs on every retry attempt exactly like the hash write it sits beside. `DisabledAt` is excluded on purpose: disabling is an operator's decision, and no proof of the password should be able to reverse it — only `EnableUser` does. `gitnexus impact` had already flagged `setPassword` HIGH risk (3 direct dependents) before this task's first commit; this fix round is exactly that risk materializing into a real, reviewed change, confirmed via `gitnexus detect_changes` (unstaged) afterward: `setPassword` genuinely touched, the six affected processes are exactly `ChangePassword`/`ResetPassword`/`SetInitialPassword`'s `HashPassword`/`UpdateUserWithRetry` steps (the expected blast radius), and `UserStore`'s four flagged symbols are the same doc-comment-line-shift false positive the original T502 commit already produced for the same reason (`user.go`'s field comments grew again) — confirmed zero occurrences via `grep -c` against the diff. New tests `TestResetPasswordClearsLockoutButNotDisable` (also pins the `DisabledAt`-survives-a-reset distinction directly, locking then disabling the same account), `TestChangePasswordClearsLockout`, `TestSetInitialPasswordClearsLockoutFields` (passwordless account; a no-op-on-zero-values check, since ordinary use can't put a nonzero `FailedLoginAttempts` on a passwordless user — `VerifyPassword`'s passwordless branch never calls `recordFailedLogin`). Mutation-tested: temporarily removed the two new lines from `setPassword`'s mutate callback — all three new tests failed identically and as predicted (`Login after ResetPassword/ChangePassword/SetInitialPassword: sulis: account locked`); reverted and confirmed green again |

---
| T503 | `RefreshSession` mints a **new session ID** and a new store row (via `CreateSession` then `DeleteSession` on the old row), rather than updating the existing row's `TokenHash`/`ExpiresAt` in place | The brief posed this explicitly as an open decision. `SessionStore` has no primitive to rewrite a session's token and expiry in place — only `TouchSession`'s `lastSeen`/`idleExpires` pair and `UpdateAuthenticatedAt`'s single field do partial updates, and Appendix A's `SessionStore` shape (written before this task) names neither a token-rewrite method nor a reason to add one. Building a fresh row from `CreateSession`+`DeleteSession`, both of which already exist, avoids adding a third narrow-purpose update method to the store contract for the sake of one caller. Rotating the ID is also a small defense-in-depth win: a previously-leaked session ID stops referring to anything live the instant the call succeeds. The new row is created *before* the old one is deleted specifically so there is no window where neither token validates |
| T503 | `RefreshSession` preserves `AuthenticatedAt` (does not reset it to `time.Now()`) | The brief's explicit requirement: "a refresh is not a re-authentication." Resetting it would silently extend how long a rotated-but-not-freshly-authenticated session keeps passing `RequireRecentAuth`, defeating the step-up gate T501 built. `UserID`/`Method`/`CreatedAt`/`IP`/`UserAgent`/`Metadata` are carried forward for the same "this is a continuation, not a new login" reason; only `ID`, `TokenHash`, `ExpiresAt`, `LastSeenAt`, and `IdleExpiresAt` are fresh |
| T503 | `IssueSession`/`IssueSessionUnchecked` do **not** grow a `RequestInfo` parameter; sessions minted through them get `RequestInfo{}` (empty `IP`/`UserAgent`), passed as a literal at `issueSessionForUser`'s one call to `createSession` | The brief flagged this as needing a decision. Appendix A is the signature authority and names no `ri` parameter for either method; both are already-shipped public API (`IssueSession` since T305, `IssueSessionUnchecked` since T305) with zero non-test callers in this repo, so adding one would be pure API churn for a Low-severity finding (C10) with no in-repo caller to migrate and validate the change against. The alternative — threading `ri` through anyway — was rejected because neither method's caller population (canonically, code that just finished a passkey ceremony) is guaranteed to have a `RequestInfo` any more readily than any other call site; if a future task finds a real need, Appendix A can be amended then with an actual caller to design against, rather than speculatively now |
| T503 | `Sulis.ListUserSessions` strips `TokenHash` to `""`; `SessionStore.ListUserSessions` (the store method) does **not** — it returns `TokenHash` exactly as stored, same as `GetSessionByTokenHash` | The security property the brief names ("ListUserSessions must never return token hashes") is about what an *application* can observe through the facade, not about what a store is allowed to persist or return internally — a store that can't return its own `TokenHash` on request couldn't satisfy `GetSessionByTokenHash` either. Stripping once, at the one facade method that lists sessions for display, is simpler to audit and to keep correct than requiring every current and future store implementation to remember to blank a field on this one method (and no other) |
| T503 | No `Sulis`-level wrapper for `DeleteUserSessionsExcept` (no `RevokeAllSessionsExcept` or similar) | The brief asked to check whether Appendix A names a service method for "sign out everywhere else" and, if not, document the store method's intended use rather than add one. It doesn't — Appendix A's function list (ratified at T002) has no such method. The facade-level path to the same outcome already exists by composition: `ListUserSessions` to enumerate, `RevokeSession` per entry to revoke. `DeleteUserSessionsExcept` stays a store-level primitive, documented for an application implementing its own store that wants a single-query version instead of the loop; adding a thin `Sulis` wrapper now would be surface added ahead of a demonstrated need, for a Low-severity finding (C10) |
| T503 | Touch-throttle interval: `IdleTimeout / 4` when `WithIdleTimeout` is configured, else a fixed `5 * time.Minute` (both in `session.go`, `sessionTouchIntervalDivisor`/`defaultSessionTouchInterval`) | The brief required *a* default, derived from the idle timeout or a constant, with the cost rationale documented. Deriving from `IdleTimeout` when one is configured keeps the staleness bound proportional to what actually matters operationally — a session in steady use is never more than a quarter of the configured idle window behind its true last-use, regardless of how short or long that window is. Without idle expiry configured there is no timeout to stay proportionally ahead of, so a fixed interval bounds `LastSeenAt` staleness purely for the device-management-screen use case; 5 minutes was chosen as generous enough to cut writes by roughly two orders of magnitude against a typical multi-request-per-minute session while still reading as "just now" on a UI |
| T503 | `ValidateSession` checks `IdleExpiresAt` before `ExpiresAt`, and both check-and-delete branches are separate `if` statements rather than one combined condition | The brief's test names the idle-expiry outcome distinctly ("fails validation BEFORE `ExpiresAt`"). Checking order has no behavioral consequence here — both branches return the identical `ErrSessionExpired` after the identical `DeleteSession` cleanup — but keeping them as two guard clauses (rather than an `||`) keeps each condition's own doc comment attached to its own line, and keeps a future change to only one of the two expiry mechanisms from having to touch a combined boolean expression |
| T503 | `ReAuthenticate` (`stepup.go`) now calls `accountStatus`, checked after rate-limiting but before the password comparison | Discharges the T501 Deferred row, assigned to this task as a candidate. Placed after `allow`/`allowIP` so the rate limiter is still consulted on every call (consistent with every other rate-limited entry point), but before `verifyPassword` so an Argon2 hash is never spent on a call that cannot succeed regardless of the password. Unlike `VerifyPassword`'s oracle-ordering rule (status must be checked only *after* an unauthenticated caller proves the password, so a wrong-password guess can't be used to learn account state), `ReAuthenticate` has no equivalent caller to protect against: its caller already holds a valid `*Session` naming this exact account, which is proof enough that the account exists, so there is nothing gained by deferring the check past the password comparison |
| T503 | `createSession` grew a fifth parameter (`ri RequestInfo`) rather than a struct or reusing `Authentication` | Same reasoning T501 already recorded for the `method`/`authenticatedAt` pair it added: every call site already has an in-scope `RequestInfo` (a real one, or the zero value) close at hand, so a wrapper struct adds an allocation and a name to thread through for no readability gain, and `Authentication` still isn't universally available at every call site (`Register`, `CompleteTwoFactor` have none) |
| T503 (fix round 1) | **CRITICAL finding from task review, fixed: `RefreshSession` now deletes the OLD session row FIRST and returns its error (fail closed) if that delete fails, and only then reloads the user and checks `accountStatus`, before minting the new row** — reversing the original commit's create-then-delete-with-discarded-error order | Reviewer found a deterministic bypass: the original order let a caller holding a stale `*Session` from before a revocation (`RevokeSession`, `RevokeAllSessions`, or a device evicted via this task's own `ListUserSessions` screen) call `RefreshSession` and mint a brand-new working session anyway — `CreateSession` never checks whether the old row still exists, and the subsequent `DeleteSession`'s `ErrSessionNotFound` was silently discarded. A second, independent bypass existed alongside it: `RefreshSession` was the only issuance-shaped path that never called `accountStatus`, so a disabled account whose session row happened to survive (e.g. `DisableUser`'s own revocation call erroring while its `DisabledAt` stamp still landed) could still refresh into a fresh session. Fixed both: delete-first with the error propagated verbatim closes the un-evict bypass; loading the user and checking `accountStatus` before minting closes the disabled-account gap. The crash window this creates — a crash between the successful delete and the create logs the caller out — is deliberately accepted as the safe direction to fail in, the same trade `consumeToken`/passkey's `ConsumeChallenge` already make ("failures burn the token") |
| T503 (fix round 1) | `RefreshSession` carries `IP`/`UserAgent` forward from the caller-supplied (possibly stale) `*Session` rather than re-deriving them, since Appendix A gives the method no `RequestInfo` parameter to re-derive them from | This was already true in the original commit but undocumented as a Decision — flagged as Minor 1 in task review. Consequence worth stating plainly: a long-lived session that gets refreshed repeatedly from a genuinely different IP over its life will keep showing whatever IP/UserAgent it was issued with in a `ListUserSessions` device listing, even while `LastSeenAt` looks current from `ValidateSession`'s touch — the two fields drift apart in staleness for different reasons (one is fixed at issuance, the other updates on every throttled touch). An application that wants IP drift detection on refresh would need to re-derive `RequestInfo` itself and call a lower-level primitive, which none currently exists for; not built here since nothing in Appendix A or the brief asked for it |
| T504 | `needsRehash(encoded string, want Argon2Params) bool` compares only the three Argon2 cost dimensions (Memory, Iterations, Parallelism), not SaltLength/KeyLength | Memory/Iterations/Parallelism are what Argon2 itself calls the cost parameters and what an operator actually raises to get more resistance to offline cracking; SaltLength/KeyLength are properties of the hash bytes that already exist on disk, not something a stored hash could be "weaker" in relative to a live config in the same sense. Comparing them would risk flagging hashes for rehash on a KeyLength change that isn't a security-motivated cost increase |
| T504 | The rehash call sits in `VerifyPassword` right after the `if !ok` correctness check, not inside `Login` or `completeFirstFactor` | `Login` and `RedeemMagicLink`'s first-factor step and `ReAuthenticate`'s step-up re-auth all eventually call `verifyPassword` through different call paths, but `VerifyPassword` is the one choke point every password-comparison caller in this package shares (`Login` calls it directly), so putting the upgrade here upgrades every path from one change. Placing it after `if !ok` rather than before means only a successful verification can ever reach it — a wrong password, unknown email, or passwordless account (both of which return before reaching the real `user.PasswordHash` comparison) never rehashes, preserving the timing-equalization property named in `VerifyPassword`'s own doc comment |
| T504 | Rehash failures (guard loss, exhausted retries, any store error, `hashPassword` itself failing) are swallowed inside `rehashPassword`, not surfaced to `VerifyPassword`/`Login`'s caller | The password already verified correctly by the time `rehashPassword` runs; the upgrade is a strictly-better-if-it-lands optimization, not a correctness requirement, so failing an otherwise-successful login over it would make the login *less* reliable in exchange for a marginally slower future comparison. The next successful login against the still-weak hash tries again, so nothing is permanently lost, only deferred |
| T504 | Three `// TODO(T509): emit rehash event` markers left in `rehashPassword` (success, guard-loss/store-error, hash-failure) rather than adding a bespoke logging hook now | T509 (security event sink) has not landed yet; inventing a second, ad hoc event mechanism here that T509 would have to reconcile with or replace is worse than leaving a named marker at each of the three outcomes T509's sink will eventually want to distinguish |

## Deferred

Anything consciously not done, so T704 can account for it. Empty is good.

| Item | Task | Reason | Revisit? |
|---|---|---|---|
| ~~`UserStore.UpdateUser` email-uniqueness enforcement isn't tested against real store implementations~~ **DISCHARGED by T401** | T401 | The in-memory test double enforced it (T107 fix round 1: `memUserStore.UpdateUser` in `sulis_test.go`), so `sulis`'s own tests held, but only a conformance suite could hold real SQL/etc. implementations to the same contract. `storetest.RunUserStore` now does: `UpdateUserRejectsAnotherUsersEmail` asserts a write colliding with a different user's live row returns `ErrUserAlreadyExists` and moves neither row, `UpdateUserKeepingItsOwnEmailIsNotACollision` pins the row-being-written exclusion, and `ConcurrentUpdateUserOntoOneEmailHasExactlyOneWinner` races eight separate users onto one free address and requires exactly one winner — the two-accounts-confirming-the-same-staged-address case `Version` cannot help with. Shown to bite: a store identical to `memstore.UserStore` minus the uniqueness check on `UpdateUser` fails the first at once and the third at iteration 0 | No — closed |
| The in-memory test doubles in `sulis_test.go`, `passkey/passkey_test.go`, `totp/totp_test.go`, and `recovery/recovery_test.go` duplicate `memstore` and are not held to `storetest` | T401 | Not a choice: they are internal test packages and `memstore`/`storetest` import the packages under test, so importing either is `import cycle not allowed in test`. See the T401 Decisions row. The practical risk is drift — a double could be quietly weakened and the package's own tests would still pass while `memstore` stayed correct. Each double now carries a comment naming `memstore` as canonical and saying why it cannot be imported, which is mitigation, not a guarantee | Yes — the real fix is converting each package's fake-using tests to external `_test` packages so they can import `memstore`, leaving only genuinely-internal tests behind. Sizeable and mechanical; a natural fit for T702 (package docs and examples) or T704. Alternatively T601/T602 (reference SQL stores) could make the point moot for the root three by giving those tests a real store to run against |
| `ErrChallengeFailed` swallows go-webauthn's diagnostic detail | T206 | The `Finish*` methods wrap library errors with `fmt.Errorf("%w: %v", ErrChallengeFailed, err)`. `%v` on a `*protocol.Error` prints only its `Details`, and the three authenticator-data checks (missing UV, missing UP, RP ID hash mismatch) all share one `Details` string — so an operator cannot tell "the user skipped their PIN" from "this assertion was minted for another relying party", and the `*protocol.Error` is not recoverable via `errors.As` either because it is not wrapped with `%w`. Not a security defect (the rejection is correct in every case) but a real diagnosability gap; a relying party triaging failed logins is exactly who needs the distinction | Yes — T509 (security event sink) is the natural home: emit the underlying `DevInfo` on the event rather than widening the returned error, so the detail reaches operators without reaching the client |
| `VerifyEmail` returns a stale `*User` when it loses a verification race | T206 | Found by `TestVerifyEmailIsANoOpWhenAnotherRequestVerifiesFirst`. `stampEmailVerified` copies the persisted row back over the caller's `*User` only on its success path; on the already-verified path it returns `nil` early, so `VerifyEmail` hands back the user as it read it — with `EmailVerifiedAt == nil` — even though the account *is* verified and the call correctly reported success. The store is right; only the returned struct is stale, and only in the narrow window where two verification links for the same address are redeemed concurrently. Left alone here because T206 is a test-only task | Yes — a one-line reload (or copying the row back on the `errAlreadyVerified` path too) fixes it; fold into T503 (session/user lifecycle) or T704 |
| `passkey.NewService` doesn't validate an empty `RPID`/`RPDisplayName`; it constructs cleanly and only fails at the first ceremony. Behavior is pinned by `TestBeginCeremoniesRejectIncompleteRelyingPartyConfig` | T206 | Test-only task; documenting the deferred gap rather than fixing it | Candidate: T702 docs, or a small validation follow-up. Revisit at T704 |
| ~~`ReAuthenticate` (`stepup.go`) does not call `accountStatus` — it can still refresh `AuthenticatedAt` for a disabled or locked account's already-held session~~ **DISCHARGED by T503** | T502 | `stepup.go` is outside T502's file list, and `ReAuthenticate` never issues a session (unlike `CompleteTwoFactor`, which does and was brought into scope — see the T502 Decisions row), so "every issuance path" doesn't clearly cover it. Low practical exposure: `ValidateSession` already blocks the same disabled account's session on every subsequent call regardless, so a quietly-succeeding `ReAuthenticate` cannot be used to keep the session usable past that check | T503 added the check (see its Decisions row) and a regression test, `TestReAuthenticateRejectsDisabledAccount`, mutation-tested by removing the call and confirming the test fails |
| ~~Automatic-lockout bookkeeping (`FailedLoginAttempts`/`LockedUntil`) is cleared only by a successful `VerifyPassword`, not by `ChangePassword`/`ResetPassword`/`SetInitialPassword` (`setPassword`)~~ **DISCHARGED by T502 fix round 1** | T502 | Task review upgraded this from a filed Deferred row to an IMPORTANT finding: with `WithFailureLockout` enabled, an attacker can lock a victim out with nothing but repeated wrong guesses, and the victim's own recovery (`ResetPassword`'s out-of-band token — a *stronger* identity proof than a login password) would otherwise leave the account waiting out `maxBackoff` anyway, worsening the exact denial-of-service risk the feature's default-off posture exists to limit — made worse by the README's own advice to configure a long `maxBackoff`. Fixed: `setPassword`'s existing `updateUserWithRetry` mutate callback now also clears `FailedLoginAttempts`/`LockedUntil` (no extra store round trip), covering all three callers. `DisabledAt`/`DisabledReason` are deliberately NOT touched — see the new T502 Decisions row | No — closed |

---

## Session log

Newest last. One line per commit: date, task, what landed.

| Date | Task | What landed |
|---|---|---|
| 2026-08-17 | — | Audit written (`docs/security-audit-2026-08-17.html`), plan and progress files created |
| 2026-08-17 | T001 | CI hardened: SHA-pinned actions, `permissions: contents: read`, Go matrix (1.25.x + stable), gofmt gate, pinned staticcheck/gosec/govulncheck, atomic coverage, Dependabot. Fixed 7 gosec findings (2 real fixes in `password.go` and `totp/totp.go`, 3 suppressed with reasons) |
| 2026-08-18 | T002 | Appendix A ratified as written; four open questions answered (explicit `RequestInfo`, `User.Version`, separate `store/sql` module, `New` returns an error). NFKC dependency question deferred to T505 |
| 2026-08-18 | T101 | `User.Version` optimistic concurrency + `ErrConcurrentUpdate`; `updateUserWithRetry` helper; `setPassword` and `stampEmailVerified` no longer write whole rows from stale reads; `setPassword` takes a re-checked guard; regression test proves the resurrection bug is fixed |
| 2026-08-18 | T102 | `SecondFactorChecker` (required by `New`), `NoSecondFactors`, `LoginResult`, `AuthMethod`, `RequestInfo`; `New` returns an error; `Login` returns `*LoginResult` and fails closed on checker errors; `createSession` moved to `issue.go`. Mutation-tested: the A1 regression test fails if the check is bypassed |
| 2026-08-18 | T103 | `RedeemMagicLink` returns `*LoginResult` and routes through `completeFirstFactor`, closing the mailbox-access-defeats-2FA bypass. Verification is stamped before the branch so a 2FA user can still verify by magic link |
| 2026-08-18 | T104 | `Session.Token` removed; raw token returned beside the session by `createSession`, `Register`, `IssueSession`. `CompleteTwoFactor` now returns `*LoginResult`. Reflection test guards the field |
| 2026-08-18 | T105 | `passkey.NewService` takes options; `WithUserVerification` defaults to `required` and is set on the RP config and both login ceremonies. Mutation-tested: without it the session records UV `""` |
| 2026-08-18 | T106 | `MemoryLimiter` token bucket is now the default; `WithoutRateLimiting()` to opt out; per-account and per-IP budgets wired into password, reset and magic-link choke points; key tracking bounded |
| 2026-08-18 | T107 | `changeemail.go`: `ChangeEmail`/`ConfirmEmailChange`, `User.PendingEmail`, `TokenPurposeEmailChange`. Confirmation swaps the address, clears `PendingEmail`, re-stamps `EmailVerifiedAt` with a fresh timestamp, and revokes all sessions plus purges password-reset and two-factor tokens. Mutation-tested: reverting the session/token purge or the `token.Email == PendingEmail` check each fail the corresponding regression test. Phase 1 complete |
| 2026-08-18 | T107 (fix round 1) | Closed a cross-account race review found: two accounts confirming the same staged address could both pass `ConfirmEmailChange`'s `GetUserByEmail` pre-check before either write landed, since `Version` only guards one row. `UserStore` GoDoc now mandates storage-layer email-uniqueness enforcement (`ErrUserAlreadyExists` from `CreateUser`/`UpdateUser`); `memUserStore.UpdateUser` (test double) now enforces it; new test `TestConfirmEmailChangeAbortsIfAnotherAccountWinsTheRace` interleaves two confirmations via the existing `beforeUpdate` hook. Mutation-tested: reverting the test-store enforcement fails only that test |
| 2026-08-18 | T201 | `ChallengeStore.GetChallenge`/`DeleteChallenge` replaced by `ConsumeChallenge(ctx, key) ([]byte, error)`, an atomic fetch-and-delete (GoDoc names Redis `GETDEL` and SQL `DELETE ... RETURNING` as reference implementations). All four `Finish*` methods now consume before verifying, so a failed verification still burns the challenge (documented as the same safe-direction policy as `consumeToken`). `BeginLogin` now returns `(*protocol.CredentialAssertion, string, error)` — a ceremony ID like `BeginDiscoverableLogin` already did — and `FinishLogin` takes that ceremony ID, keying login challenges per-ceremony instead of per-user so a second login ceremony can't clobber the first device's challenge. TDD: wrote `TestFinishDiscoverableLoginConsumesChallengeExactlyOnce` (200 iterations, shared start gate, `-race`) against the unmodified get-then-`defer`-delete code first — it failed deterministically at iteration 0/1 every run (0 of 2 racers got `ErrChallengeExpired`; both proceeded past the gate), and separately tripped the race detector on the un-mutexed in-memory test double's map. Mutation-tested: removing the `delete` from the fixed `ConsumeChallenge` reproduces the same iteration-0 failure; restored |
| 2026-08-18 | T202 | `BeginRegistration` now loads the user's existing credentials from the store and passes them to go-webauthn as `webauthn.WithExclusions(...)`, populating `Response.CredentialExcludeList` (`excludeCredentials`) so the browser can warn on a duplicate authenticator instead of silently creating a second credential. `User.Credentials` removed from the exported struct; a new unexported `webauthnUser` (wraps `*User` plus a store-loaded `[]Credential`) implements `webauthn.User` and is built fresh inside `BeginRegistration`/`FinishRegistration`/`BeginLogin`/`FinishLogin`/`FinishDiscoverableLogin`. Also fixed `challengeKey`'s doc comment, which named only "register"/"login" and omitted "discover" (deferred minor from T201's review). TDD: `TestBeginRegistrationExcludesExistingCredentials` written first and confirmed to fail (`CredentialExcludeList` had 0 entries) before the store lookup and `WithExclusions` wiring were added |
| 2026-08-18 | T203 | `WithResidentKey(protocol.ResidentKeyRequirement)` (default `required`) sets `ResidentKey` and the legacy `RequireResidentKey` bool on the RP config; `BeginRegistration` also requests the `credProps` extension unconditionally. `Credential.Discoverable` (new field) is populated in `FinishRegistration` from the client's `credProps.rk` extension output — go-webauthn v0.17.4 exposes no other resident-key signal on the finished credential, so `FinishRegistration` now calls `protocol.ParseCredentialCreationResponse` + `s.wa.CreateCredential` directly instead of `s.wa.FinishRegistration`, solely to reach `ClientExtensionResults`. TDD: wrote the `ResidentKey`/`RequireResidentKey` default test and the credProps-extension-request test first (both failed — `WithResidentKey`/`Discoverable` undefined), then the two `FinishRegistration` persistence tests (Discoverable true/false) against the real W3C "none ES256" spec test vector, with a synthesized `clientExtensionResults.credProps.rk` value layered on top (credProps is a client output, not part of the signed attestation object, so it can be added independently). Mutation-tested: reverting `Discoverable`'s assignment to a literal `false`, reverting the `ResidentKey`/`RequireResidentKey` default, and removing the `credProps` extension request each fail their corresponding test; all reverted. Also fixed the stale "All 10 completed tasks" carry-forward count (was already wrong before this task landed; correct count is now 12) |
| 2026-08-18 | T204 | `WithMaxCeremonyBody(int64)` (default 64 KiB) caps ceremony response bodies. `FinishRegistration`/`FinishLogin`/`FinishDiscoverableLogin` moved to new `passkey/http.go` as thin wrappers: each reads `r.Body` via `http.MaxBytesReader` then delegates to a new `[]byte`-taking core in `passkey.go` — `FinishRegistrationResponse`/`FinishLoginResponse`/`FinishDiscoverableLoginResponse` — which parse via `protocol.ParseCredentialCreationResponseBytes`/`ParseCredentialRequestResponseBytes` and call `wa.CreateCredential`/`wa.ValidateLogin`/`wa.ValidateDiscoverableLogin`. `passkey.go` no longer imports `net/http`. Each core method rejects `len(body) > max` as its first step, before consuming the challenge. TDD: wrote the oversized-body tests first against the not-yet-existing `[]byte` methods (compile failure, then a real assertion failure once the methods were stubbed in) before adding the cap. Mutation-tested twice: (1) neutering `checkCeremonyBodySize` to always return nil fails the three `[]byte`-core oversized-body tests but *not* the `net/http` one, since `MaxBytesReader` catches it independently — confirms the two layers are genuinely independent; (2) reverting `http.go` to a plain `io.ReadAll(r.Body)` (no cap) is caught by `TestFinishRegistrationCapsBodyReadRatherThanReadingItAll`, which sources the body from a 10×-oversized streaming reader and asserts on bytes actually read (655360 read vs. an expected ≤65537), not merely on the returned error — the original oversized-`[]byte`-body test alone did *not* catch this mutation, since the body was already fully materialized before the request was built. All reverted after confirming. README passkey section updated |
| 2026-08-18 | T205 | `Credential` gains `Name`, `Transports`, `BackupEligible`, `BackupState`, `LastUsedAt`; `FinishRegistrationResponse` persists the latter three from `waCredential.Transport`/`.Flags`. `Store.UpdateCredentialSignCount` replaced by `UpdateCredentialAfterLogin(ctx, credentialID, signCount, backupState, lastUsedAt)`, called from `finishLoginCredential`. New `Store.DeleteCredentialsByUserID` and `RenameCredential` (returns `ErrPasskeyNotFound` for an unknown id). New `Service.DeleteCredential(ctx, userID, id string, opts DeleteOptions)` enforces a last-credential guard — rejects with `ErrLastCredential` unless `opts.AllowLast` — since `passkey` can't see whether the account has another auth factor. Also fixed `toWebAuthnCreds`, found while wiring the backup flags through: it previously dropped `Flags`/`Transport` entirely, which would have made go-webauthn's own login verification reject every subsequent login for a genuinely backup-eligible credential (its `BackupEligible`-consistency check compares against exactly what this function feeds it). TDD: all new/changed symbols referenced by tests before existing in `store.go`/`passkey.go`, confirmed as a compile failure, then implemented. Mutation-tested four ways, each reverted after confirming the corresponding test (and only that test, where checked) fails: (1) `toWebAuthnCreds` not setting `Flags`/`Transport`; (2) `finishLoginCredential` calling `UpdateCredentialAfterLogin` with `backupState=false, lastUsedAt=time.Time{}` hardcoded; (3) the last-credential guard removed from `Service.DeleteCredential`; (4) `FinishRegistrationResponse` not populating `Transports`/`BackupEligible`/`BackupState`. README passkey section and Store Contracts bullet updated. Phase 2: T201–T205 done, T206 remains |
| 2026-08-18 | T205 (fix round 1) | Task review found a CRITICAL TOCTOU race in the last-credential guard: `Service.DeleteCredential`'s original get-count-then-delete was two unsynchronized steps, so two concurrent deletes of a user's last two credentials could both pass the guard and leave zero credentials. Fixed by moving the guard into `Store.DeleteCredential`, now `DeleteCredential(ctx, userID, id string, allowLast bool) error`, documented as required to be one atomic operation (SQL/in-memory reference implementations given); `Service.DeleteCredential` is now a thin delegator. `fakeStore` gained a `sync.Mutex` and an atomic `DeleteCredential` implementation. New `TestDeleteCredentialGuardIsAtomicUnderConcurrentDeletes` (200 iterations, shared start gate, `-race`, modeled on T201's challenge-consumption test) proves exactly one of two racing deletes succeeds. Mutation-tested: reintroducing the TOCTOU gap in `fakeStore.DeleteCredential` reproduced the race at iteration 16 (2 succeeded, 0 rejected) under `-race`; reverted. Four pre-existing `DeleteCredential` tests updated from call-count to outcome assertions. README passkey section and Store Contracts bullet updated for the new atomicity requirement. Full suite (`gofmt`, `go build`, `go vet`, `go test -race -count=1 ./...`, `staticcheck`, `gosec`) green after the fix |
| 2026-08-18 | T206 | New `passkey/testauth_test.go`: a forging test authenticator (`testAuthenticator`, `forgingFixture`) that mints signed WebAuthn registration and assertion responses with controllable UP/UV/BE/BS flags, RP ID, origin, challenge, sign count, user handle, credential ID, and signing key — built on the stdlib plus go-webauthn's own `webauthncbor`, so `go.mod`/`go.sum` are unchanged (verified with `go mod tidy`). No production code was touched. Every ceremony rejection path now has a test asserting its specific outcome: UV-absent (login, discoverable login, registration), UP-absent, clone warning via a stale sign count, origin mismatch (login and registration), RP ID mismatch (login and registration), challenge mismatch, assertion signed by another key, credential ID not owned by the user, malformed authenticator data, malformed CBOR attestation object, unparseable stored COSE public key, discoverable credential owned by another user, discoverable credential unknown to the store, and replay of a consumed challenge in all three ceremonies. The two outstanding test debts are discharged (see the amended T105 and T205 Decisions rows). Also: concurrency parity — added exactly-once challenge-consumption tests for `FinishLogin` (8 racers, asserting 1 success + 7 `ErrChallengeExpired`) and `FinishRegistration`, matching T201's discoverable-login test; error-path parity — `fakeStore`/`fakeChallengeStore` gained injectable failures, covering `GetCredentialsByUserID` in `BeginRegistration`/`BeginLogin`/`FinishLoginResponse`, `SaveChallenge` in all three `Begin*`, `SaveCredential`, `UpdateCredentialAfterLogin`, `GetCredentialByID`, corrupt session data in all three `Finish*`, non-size body read failures in all three `net/http` wrappers, and `NewService` with no RP origins. Mutation-tested seven ways, each reverted after confirming: (1) UV default weakened to `VerificationPreferred` → all three UV tests fail; (2) `toWebAuthnCreds` dropping `Flags` → backup-eligible login fails with "Backup Eligible flag inconsistency"; (3) clone-warning check removed → clone test fails; (4) the discoverable handler's `cred.UserID != string(userHandle)` check removed → only the cross-user test fails (surgical); (5) an extra origin added to `RPOrigins` → both origin tests fail; (6) `RPID` hardcoded to the attacker's domain → both RP ID tests fail; (7) `ConsumeChallenge` made non-atomic → the login/registration/discoverable concurrency tests fail. **Coverage: `passkey` 78.5% → 98.3%**, now the highest in the module (root 86.0%, `recovery` 92.1%, `totp` 86.1%); the only uncovered statements left in `passkey` are three unreachable `json.Marshal(webauthn.SessionData)` error branches. Verified the CodeGraph-flagged root-package gaps: `issueSessionForUser` and `requireVerifiedEmail` were already at 100% through their callers, but `stampEmailVerified` was at 75% — added four `sulis_test.go` tests (concurrent-verification no-op, update failure, session-revocation failure, both user-lookup failures) taking it and `VerifyEmail`/`CreateEmailVerificationToken` to 100% and the root package 84.6% → 86.0%. Two non-blocking findings recorded in Deferred. Phase 2 complete |
| 2026-08-18 | T301 | `totp.Validate` now returns `error` only — nil means valid, `ErrTOTPInvalid` means a wrong code (reused, not new: it already existed for `ConfirmEnrollment` but was unused by `Validate`), `ErrTOTPReplayed`/`ErrTOTPNotVerified`/`ErrTOTPNotEnrolled`/`ErrTOTPRateLimited` stay distinct via `errors.Is`. Closes the `(false, nil)`-reads-as-success bypass described in the task brief. TDD: added `TestValidateRejectsWrongCodeWithError` against the not-yet-changed two-return signature first — it failed to compile ("assignment mismatch: 1 variable but svc.Validate returns 2 values"), confirming red — then changed the signature and updated all 13 call sites across `totp_test.go` (each `ok, err :=`/`_, err :=` pattern collapsed to a single `err`) plus the README TOTP example and subpackage description. Mutation-tested: reverting the wrong-code branch to `return nil` fails both `TestValidateRejectsWrongCodeWithError` and `TestEnrollAndValidate`'s wrong-code assertion; reverted after confirming. `gitnexus impact` on `Validate` (upstream) reported 0 callers outside the package before the change, and `gitnexus detect_changes` after confirms only the expected symbols (`totp.go`, `totp_test.go`, README's totp section) were touched. Also added the T206 deferred-finding row (passkey `NewService` doesn't validate empty `RPID`/`RPDisplayName`) to the Deferred table, carried over from that task's review and recorded here since this task also touched this file |
| 2026-08-18 | T303 | `SessionStore.DeleteSession` gains a `userID` parameter: `DeleteSession(ctx, userID, id string) error`, scoped to both columns and required to return `ErrSessionNotFound` on zero rows affected (interface GoDoc states the SQL reference shape, `DELETE FROM sessions WHERE id = ? AND user_id = ?`). `RevokeSession(ctx, userID, sessionID string) error` passes its caller's own userID through, closing the cross-user revocation bug described in the task brief (a session-management UI wired to the old `RevokeSession(ctx, sessionID)` let user A revoke user B's session by guessing/leaking its ID). `ValidateSession`'s expired-session cleanup updated to pass `validated.UserID` alongside `validated.ID`. All three test session stores (`memSessionStore`, `observingSessionStore`, `sharedSessionStore`) updated; `memSessionStore`/`observingSessionStore` now genuinely enforce the scoping, `sharedSessionStore`'s delete stays a no-op stub (never exercised in a scoping-sensitive test). TDD: `TestRevokeSessionRejectsCrossUserAttempt` written first against the unchanged two-arg signature — confirmed red via `go vet` compile failure (`too many arguments in call to s.RevokeSession`), this series' established compile-failure-as-red convention — then the signatures and store scoping were implemented. Mutation-tested: temporarily reverting `memSessionStore.DeleteSession` to its unscoped `delete(s.sessions, id)` reproduces the predicted failure (`expected ErrSessionNotFound for cross-user revoke, got <nil>`); reverted after confirming. See the T303 Decisions row for why the ownership check lives in the store's atomic delete rather than a `Service`-level pre-check. `gitnexus detect_changes` after the change reported only `session.go`, `sulis.go`, `sulis_test.go` touched (plus expected line-shift noise on unrelated symbols below the edited interface), medium risk, consistent with a `SessionStore` interface signature change. README's `SessionStore` Store Contracts bullet and `ValidateSession` Core Flows section both updated. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 4 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues) |
| 2026-08-18 | T302 | `totp.Store` now separates a user's active (verified) credential from a pending (unverified) enrollment, per Appendix A / the Ratified API. New: `ErrTOTPAlreadyEnrolled`; `Store.GetActiveTOTP`/`GetPendingTOTP`; `Store.EnrollPending` (atomic active-credential guard) / `ReplacePending` (same write, no guard); `Store.ConfirmEnrollment(ctx, userID, pendingID string, counter uint64) (*Credential, error)` (atomic pendingID compare-and-swap promotion, carrying `LastUsedCounter` forward monotonically — never lower than the credential it replaces); `Service.ReplaceEnrollment(ctx, userID, accountName string) (secret, uri string, err error)`. `Service.Enroll` now refuses with `ErrTOTPAlreadyEnrolled` when an active credential exists (previously it unconditionally overwrote whatever `SaveTOTP` had on file for the user, verified or not — the actual bug: no concurrency was even required to trigger it, a single `Enroll` call after enrollment was enough). See the two T302 Decisions rows above for the store-contract shape and where the atomicity lives. TDD: wrote the new tests (`TestEnrollReturnsErrTOTPAlreadyEnrolledForVerifiedUser`, `TestPendingEnrollmentDoesNotDisturbActiveCredential`, `TestReplaceEnrollmentSupersedesActiveEnrollmentExplicitly`, `TestConfirmEnrollmentIsAtomicUnderConcurrentEnrollPending`) and the redesigned `memTOTPStore` (active/pending maps) against the unchanged `totp.go`/`store.go` first — confirmed red via `go vet` compile failure (`undefined: ErrTOTPAlreadyEnrolled`, per this series' compile-failure-as-red convention) — then implemented `store.go`'s new `Store` interface and `totp.go`'s `Enroll`/`ReplaceEnrollment`/`ConfirmEnrollment`/`Validate`. `gitnexus impact` on `Enroll`/`ConfirmEnrollment`/`Validate`/`Store` (upstream) all reported 0 callers outside the package before editing (LOW risk); `gitnexus detect_changes` after confirms only `totp/store.go`, `totp/totp.go`, `totp/totp_test.go` were touched, all affected processes confined to the totp package's own enrollment/validate flows. Mutation-tested the clobber guard per the brief: temporarily made `Enroll` call `ReplacePending` unconditionally (matching pre-T302 behavior) — `TestEnrollReturnsErrTOTPAlreadyEnrolledForVerifiedUser` failed exactly as predicted (`Enroll() error = <nil>, want ErrTOTPAlreadyEnrolled`); reverted and confirmed green again. The concurrency regression test exercises `Store.EnrollPending`/`ConfirmEnrollment` directly rather than through `Service`, since `Service.ConfirmEnrollment`'s own `GetPendingTOTP` read (needed to validate the code) sits outside the store's atomic section — see the second T302 Decisions row for why the atomicity boundary is drawn there. `TestConfirmEnrollmentDoesNotRollBackCounter` (which re-confirmed the same pending enrollment twice) no longer applies now that confirmation consumes the pending slot exactly once; replaced with `TestConfirmEnrollmentCarriesCounterForwardAcrossReplace`, which pins the equivalent monotonic-counter guarantee across a `ReplaceEnrollment`+`ConfirmEnrollment` factor swap instead (see the fourth T302 Decisions row). Updated the README's `totp` section and Store Contracts entry for the new API and atomicity requirements. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 4 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues). `totp` coverage 86.1% → 85.7% (not chased further — out of scope for this task) |
| 2026-08-18 | T304 | `CreatePasswordResetToken` no longer returns `ErrUserNotFound` for an unregistered address — it returns `("", nil)`, closing the enumeration oracle the README previously told consumers to close themselves. New `CreatePasswordResetTokenStrict` (same signature, both take `ri RequestInfo` per Appendix A) returns `ErrUserNotFound` verbatim, for admin tooling that needs the truth. Both share one unexported `createPasswordResetToken(ctx, email, ri, strict bool)`; `strict` selects the one branch that differs. The unknown-address branch calls a new package-level `var resetTokenGenerator = generateRawToken` (burning the same random-generation + SHA-256-hashing work the known-address path spends inside `createTokenForUserWithEmail`) and discards the result — no `TokenStore.CreateToken` call happens on that path, so nothing is ever persisted to discard. The doc comment names the one residual asymmetry this can't equalize (the known-address path still writes a token row; the unknown-address path never does, since there's no user to attach one to), explicitly mirroring how `VerifyPassword`'s dummy-hash equalization documents its own residual gap. TDD: wrote `TestCreatePasswordResetTokenUnknownEmailReturnsEmptyResult`, `TestCreatePasswordResetTokenUnknownEmailPerformsEquivalentWork` (wraps `resetTokenGenerator` with a counting closure, asserts exactly 1 call, and asserts the token store's row count stayed 0), `TestCreatePasswordResetTokenStrictReturnsErrUserNotFound`, and `TestCreatePasswordResetTokenStrictKnownEmailReturnsToken` against the unchanged `sulis.go` first — confirmed red via `go vet` compile failure (`undefined: resetTokenGenerator`), this series' established compile-failure-as-red convention — then implemented. Mutation-tested per the brief: temporarily made the unknown-address branch `return "", nil` immediately, without calling `resetTokenGenerator` — only `TestCreatePasswordResetTokenUnknownEmailPerformsEquivalentWork` failed (`expected the unknown-user path to generate a token exactly once, got 0 calls`), exactly as predicted; reverted and confirmed green again. `gitnexus impact` on `CreatePasswordResetToken` (upstream) reported LOW risk, 0 callers outside the package, before editing; `gitnexus detect_changes` after reported `sulis.go`/`sulis_test.go` touched plus expected line-shift noise on unrelated downstream symbols (`ResetPassword`, `ValidateSession`, `RevokeSession`, `RevokeAllSessions`, and several test-double methods) — confirmed via `git diff` that none of those were actually modified, same false-positive pattern T303's Decisions row already documented for this tool. No existing test asserted `ErrUserNotFound` from `CreatePasswordResetToken`, so nothing broke. README's method list, Password Reset section, Magic Link cross-reference, and the "Respond identically" Operational-requirements warning all rewritten to describe the safe default plus the Strict variant instead of telling consumers to flatten the error themselves. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 4 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues) |
| 2026-08-18 | T305 | `IssueSession` now takes an `Authentication` (opaque, unexported `userID`/`method`/`at` fields) instead of a bare `userID`; the zero value is rejected with new sentinel `ErrNotAuthenticated`. New `IssueSessionUnchecked(ctx, userID string, method AuthMethod) (*Session, string, error)` is the old bare-userID behavior under a name that shows up in code review, for a factor sulis does not know about (canonically, a finished passkey ceremony). Both share one internal `issueSession(ctx, auth)` (load user by `auth.userID`, delegate to the pre-existing `issueSessionForUser`); `IssueSessionUnchecked` mints its `Authentication` via a new unexported `newAuthentication(userID, method)` constructor — the only way to produce a valid one, and not reachable from outside the package. `IssueSession`, `IssueSessionUnchecked`, `issueSessionForUser`, and `Authentication` moved from `sulis.go` into `issue.go` (matches `PLAN.md`'s File Map, "issue.go: session issuance + `Authentication`"). TDD: wrote `TestIssueSessionRejectsZeroValueAuthentication` against the unchanged bare-`userID` signature first — confirmed red via `go vet` compile failure (`undefined: Authentication`), this series' established compile-failure-as-red convention — then implemented. Mutation-tested: temporarily removed the `auth.userID == ""` guard from `IssueSession` — `TestIssueSessionRejectsZeroValueAuthentication` failed exactly as predicted (`expected ErrNotAuthenticated, got sulis: user not found` — the zero-value's empty user ID fell through to `GetUserByID` and surfaced `ErrUserNotFound` instead), confirming the guard is load-bearing; reverted and confirmed green again. `gitnexus impact` on `IssueSession`/`createSession`/`issueSessionForUser` (upstream) all reported LOW risk before editing (3, 1, and 4 callers respectively, all but one being tests); `CompleteTwoFactor` (upstream, before its comment-only touch) reported MEDIUM risk on paper but only because it has 8 test callers — no non-test caller, so safe for a comment addition. `gitnexus detect_changes` after the change reported the expected line-shift noise on `errors.go`'s sentinel block (a new var inserted mid-block shifts every symbol below it) and on `createSession`/two-factor test functions below the edited regions — confirmed via `git diff` that none of those actually changed, the same false-positive pattern T303/T304 already documented for this tool. See the T305 Decisions rows for why `completeFirstFactor`'s session branch and `CompleteTwoFactor` keep calling `createSession`/`issueSessionForUser` directly rather than routing through the new checked path — the brief's explicitly offered alternative, chosen to avoid a redundant `GetUserByID` on every login and every completed 2FA. Existing tests `TestIssueSessionReturnsValidatableSession`, `TestUnverifiedAccountCannotStartNewSessions`, and `TestIssueSessionUnknownUserReturnsErrUserNotFound` updated to construct a valid `Authentication` via `newAuthentication`, each also gaining a parallel assertion that `IssueSessionUnchecked` behaves identically (same `ErrEmailNotVerified`/`ErrUserNotFound` gating). New `TestIssueSessionUncheckedCreatesValidatableSession` pins the round-trip property for the unchecked path directly. README: method lists, the `IssueSession`/`IssueSessionUnchecked` prose, the Two-Factor code sample (now `IssueSessionUnchecked` with a comment explaining why — the app determines 2FA status itself rather than through `Login`'s `SecondFactorChecker`), the passkey section's post-`FinishLogin` call, the password-change re-login note, and the verified-email-gate bullet all updated. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 4 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues). Root `sulis` coverage 86.0% → 86.4%. **Phase 3 complete.** |
| 2026-08-18 | T401 | Two new public packages. `storetest` is the conformance suite for every store interface in the module — `RunUserStore`, `RunSessionStore`, `RunTokenStore`, `RunPasskeyStore`, `RunPasskeyChallengeStore`, `RunTOTPStore`, `RunRecoveryStore`, each `func(t *testing.T, factory func() <Interface>)` — 85 subtests, stdlib plus this module only. `memstore` is the reference in-memory implementation of all seven, 100% covered by that suite (`memstore_test.go` is an external `package memstore_test`, which is the only way to import `storetest` without a cycle). TDD per the brief: `RunTokenStore` was written first, run green against `memstore.TokenStore`, then `ConsumeToken` was deliberately split into a lookup critical section and a separate mark critical section — the suite failed at iteration 1 with "goroutines 4 and 5 both succeeded — want exactly one; the check and the mutation are not atomic", and still failed at iteration 8 with the `runtime.Gosched()` hint removed, i.e. it catches the subtle version too. That exercise was then repeated for the whole suite: eight deliberately broken stores (`UserStore` ignoring `Version`; `UserStore` with no email uniqueness on `UpdateUser`; `DeleteSession` ignoring its `userID`; a non-atomic passkey last-credential guard; a get-then-delete `ChallengeStore`; a `SaveTOTP` that lets the counter regress; a read-then-write `ConfirmEnrollment` with a two-step `DeleteTOTP`; a get-then-delete `ConsumeCode`) were each run against the suite and each failed, at iteration 0 or 1, on exactly the subtest that names their defect — 13 failing subtests across the eight, no false negatives and no false positives elsewhere. The broken variants were scratch and are not committed; the run output is quoted in `.superpowers/sdd/PLAN/task-T401-report.md`. **Discharges the T107 Deferred row** (`UpdateUser` email uniqueness now has three dedicated tests, including an eight-way race onto one address). Concurrency subtests use T201/T205/T302's shared-start-gate shape, repeat 100× (5 under `-short`), and take a fresh store per iteration. README gained a "Proving your stores correct" section (five-line example, per-interface table, factory requirements, the `-race`/`-short` note, and `memstore` as both reference reading and a working dev store) plus an intro mention. **The brief's step 4 — moving the existing fakes into `memstore` — was not done, and cannot be:** every test file in the module is an internal test package and `memstore` imports the package under test, so `import cycle not allowed in test` (verified with a throwaway probe, then removed). Each surviving double now carries a comment naming `memstore` as canonical and explaining the constraint; see the T401 Decisions and Deferred rows. One gosec G115 finding in the suite's own code (int→uint64 in the counter-monotonicity race) fixed properly by precomputing a `[]uint64` rather than suppressing. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 6 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues) |
| 2026-08-18 | T401 (fix round 1) | Task review found `memstore.UserStore` (`CreateUser`, `UpdateUser`, `GetUserByID`, `GetUserByEmail`) and `memstore.SessionStore` (`CreateSession`, `GetSessionByTokenHash`) doing shallow `cp := *user` copies on structs carrying `Metadata map[string]any` — a map copy is a reference copy, so a caller who populated `Metadata` and later mutated it, or who mutated a map obtained from a getter, reached straight into the store's persisted state and could rewrite a row without going through `UpdateUser`. That contradicted `memstore`'s own package doc in the one package adopters are pointed at as exemplary. Fixed with `cloneUser`/`cloneSession` helpers using `maps.Clone`; `cloneUser` also copies the pointed-to `EmailVerifiedAt` time, which had the identical aliasing hole (a `*time.Time` field the reviewer did not name, found while fixing the map). Rather than leaving this as a memstore-only fix, it is now the contract: `UserStore` and `SessionStore` GoDoc state that a store MUST NOT share mutable state with its callers in either direction and name both traps; `storetest`'s `ReturnedUsersAreIndependentOfStoredState` and `ReturnedSessionsAreIndependentOfStoredState` were extended with new `mutateMetadata`/`assertMetadataUnchanged` helpers to populate and mutate `Metadata` across every boundary crossing (`CreateUser`, `UpdateUser`, `GetUserByID` for users; `CreateSession`, `GetSessionByTokenHash` for sessions) plus the `EmailVerifiedAt` pointer; `memstore`'s package doc now enumerates exactly which maps, slices, and pointers are cloned and where the one-level line is drawn; README's Store Contracts section gained a paragraph. The assertions stay tolerant of a store that does not persist `Metadata` at all (nil or empty is not a failure) — the interfaces promise nothing about the field's persistence, only that it must not be aliased if it *is* persisted. The root package's `memUserStore`/`memSessionStore` doubles got matching `cloneTestUser`/`cloneTestSession` helpers so they are not weaker than the contract; safe because no existing test touches `Metadata` and every `EmailVerifiedAt` write in the module assigns a fresh pointer rather than writing through one. Mutation-tested twice, each reverted after confirming: (1) `cp.Metadata = u.Metadata` instead of `maps.Clone` → both extended subtests fail with `Metadata["role"] = mutated-by-the-caller, want member` / `Metadata["device"] = mutated-by-the-caller, want laptop`; (2) dropping the `EmailVerifiedAt` pointer copy → `ReturnedUsersAreIndependentOfStoredState` fails with "the pointer must not be shared with the store". `gitnexus impact` on `User` (upstream) reported **HIGH** risk (2 direct dependents, affected process `RedeemMagicLink`) and `Session` LOW — both edits are doc-comment-only on the struct/interface, no field, signature, or behavior change, so the blast radius is not exercised; flagged here per the project's HIGH-risk warning rule. Also recorded the honest dependency note: `storetest/passkey.go` imports `go-webauthn/webauthn/protocol`, which is already in `go.mod` and already on `passkey.Store`'s public surface via `Credential.Transports`, so the brief's "stdlib + the sulis module only" is true in spirit but not literally. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 6 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues); `memstore` still 100.0% covered |
| 2026-08-18 | T402 | Native Go fuzzing (`testing.F`) added for all four hand-rolled parsers named in the brief: `FuzzDecodeHash`/`FuzzNormalizeEmail` (new root `fuzz_test.go`), `FuzzRecoveryCanonical` (new `recovery/fuzz_test.go`), `FuzzGenerateCode` (new `totp/fuzz_test.go`, targeting the package-level `generateCode`, the function whose base32 decode the brief's "totp.generateCode" line refers to). Each target asserts a named property, not just no-panic: `FuzzDecodeHash` — no panic on arbitrary input, plus a round-trip (`hashPassword`'s own output must always `decodeHash` back out with matching params/lengths, using `testArgon2Params` for speed); `FuzzNormalizeEmail` — no panic, any accepted address is already lowercase/trimmed/≤254 bytes, and idempotent; `FuzzRecoveryCanonical` — no panic and idempotent; `FuzzGenerateCode` — no panic, a pre-epoch time is rejected with an error (the T001 `counterAt` guard), and on success the code is exactly `Digits` ASCII digits. `gitnexus impact` run upstream on all four before any editing: `decodeHash`/`normalizeEmail`/`generateCode` HIGH risk (multiple direct callers each — `Login`, `ChangePassword`, `CreateMagicLinkToken`, `CreatePasswordResetToken`, `ConfirmEnrollment`, `Validate`), `canonical` LOW risk (1 direct caller, `hashCode`); flagged per the project's HIGH-risk warning rule, though only `canonical` (LOW) was ultimately edited. TDD framing per the brief: each property was shown to bite by temporarily reverting the guarantee in production code and re-running the seed corpus (not the full fuzz loop) before any 60s run — four mutations, all confirmed and reverted (see the T402 Decisions row for the exact list, including the RFC 6238 leading-zero-code seed added specifically to make the digit-format mutation reproduce deterministically rather than by luck). Then each target ran the real `go test -fuzz='^FuzzX$' -fuzztime=60s` locally, one at a time: `FuzzDecodeHash` (~34.5k execs, clean), `FuzzNormalizeEmail` (~5.0M execs, clean), `FuzzGenerateCode` (~3.0M execs, clean) — three of four surfaced nothing. `FuzzRecoveryCanonical` found a genuine bug within its first 2 seconds: `canonical` was not idempotent for a dash immediately followed by non-space whitespace (`canonical("-\t0")` = `"\t0"`, but canonicalizing that again gives `"0"`) — see the T402 Decisions row for the root cause and fix (`recovery.go`'s `canonical` rewritten as a single left-to-right `unicode.IsSpace`/`-`-stripping pass, trivially idempotent by construction). Fixed with a checked-in crasher regression seed (`recovery/testdata/fuzz/FuzzRecoveryCanonical/3853ddfc31ee1eab`, auto-generated by the fuzzer and now run as a regression test by plain `go test`) plus a named unit test, `TestCanonicalIsIdempotentWithInteriorWhitespace` (`recovery/recovery_test.go`) — both mutation-tested by reverting to the pre-fix implementation and confirming each fails identically to the original fuzz output, then re-applying the fix. Re-ran `FuzzRecoveryCanonical` for a full clean 60s after the fix (~8.5M execs, clean). CI gained a new `fuzz` job (`.github/workflows/ci.yml`) running all four targets sequentially with `-fuzztime=10s` and `-run='^$'` (skipping the package's other tests, already covered by the `test` job) — SHA-pinned `checkout`/`setup-go` actions matching T001's existing pins, no new actions or permissions needed since `contents: read` already applies workflow-wide. `gitnexus detect_changes` after the fix reported only `canonical` as an actual production-code change (MEDIUM risk overall; `hashCode` and two test functions were flagged too but confirmed via `git diff` to be the same line-shift false-positive pattern T303/T304/T305 already documented for this tool — their line numbers moved because `canonical` grew, their bodies didn't change). Coverage bumps at T402 (root 86.4%→86.8%, `recovery` 92.1%→93.0%, `totp` 85.7%→89.7%) are the checked-in fuzz seed corpora themselves running as regression subtests under plain `go test -cover` — error branches in `decodeHash`/`generateCode` that no hand-written test happened to exercise before now do. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 6 packages, `storetest` has no tests by design), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues; gosec does not scan `_test.go` files, so the fuzz targets themselves aren't in its 7,120-line/35-file scan). **Phase 4 complete.** |
| 2026-08-18 | T501 | Step-up authentication, per Appendix A. New `stepup.go`: `RequireRecentAuth(ctx, session, maxAge) error` (returns `ErrReauthRequired`, new sentinel, if `session.AuthenticatedAt` is older than `maxAge`; zero `AuthenticatedAt` fails closed with no special-case code, since `time.Since` of the zero time vastly exceeds any real `maxAge`) and `ReAuthenticate(ctx, session, password, ri) error` (loads the user by `session.UserID`, rate-limits on `"password:"+email` — same order and budget as `ChangePassword` — verifies the password with the same dummy-hash timing-equalization `VerifyPassword` uses for a passwordless account, then on success calls the new `SessionStore.UpdateAuthenticatedAt(ctx, id, at)` and also updates `session.AuthenticatedAt` in place so the caller's already-held `*Session` reflects the refresh without a reload). `Session` gains `AuthenticatedAt time.Time` and `Method AuthMethod`, both stamped at issuance: `createSession` grew two parameters (`method AuthMethod, authenticatedAt time.Time`) threaded from every call site — `Register` (`AuthMethodPassword`, reusing the `now` already computed for the new user's `CreatedAt`), `completeFirstFactor` (the `method` it already receives, `time.Now()`), `CompleteTwoFactor` (`AuthMethodTwoFactor` unconditionally — the second factor authorized the session, not whichever method passed the first — `time.Now()`), and `issueSessionForUser`/`issueSession` (the `Authentication` proof's own `method`/`at` — see below). TDD: wrote all nine new tests (`TestRequireRecentAuth{OldStampReturnsErrReauthRequired,FreshStampReturnsNil,ZeroAuthenticatedAtFailsClosed}`, `TestReAuthenticate{CorrectPasswordRefreshesAuthenticatedAt,WrongPasswordDoesNotRefreshStamp,PasswordlessUserReturnsInvalidCredentials,ConsultsLimiter}`, `TestCreateSessionRecordsAuthenticatedAtAndMethod`, `TestCompleteTwoFactorRecordsTwoFactorMethod`) against the unchanged struct/interface first — `go vet` failed to compile (`unknown field AuthenticatedAt in struct literal of type Session`), this series' established compile-failure-as-red convention — then implemented. Mutation-tested both properties the brief named: (1) `RequireRecentAuth` with the `time.Since(...) > maxAge` check replaced by an unconditional `return nil` — `TestRequireRecentAuthOldStampReturnsErrReauthRequired` and `TestRequireRecentAuthZeroAuthenticatedAtFailsClosed` both failed exactly as predicted (`error = <nil>, want ErrReauthRequired`); (2) `ReAuthenticate` with the `UpdateAuthenticatedAt` call moved before the `if !ok` correctness check, so a wrong password still refreshes the stamp while still correctly returning `ErrInvalidCredentials` — `TestReAuthenticateWrongPasswordDoesNotRefreshStamp` failed exactly as predicted (`AuthenticatedAt changed on a failed ReAuthenticate`). Both reverted and confirmed green again. **Consumed `Authentication.at`** (T305's unexported timestamp field, minted by `newAuthentication` but never read by anything since): `issueSessionForUser` now takes `method`/`authenticatedAt` params and `issueSession` passes `auth.method`/`auth.at` through to it, so a session minted via `IssueSession`/`IssueSessionUnchecked` is stamped with the moment the proof was actually minted, not a store-round-trip-later `time.Now()`; `Authentication.at`'s field comment now says so, discharging the "why does this exist and go unread" gap T305 left open (see the T501 Decisions row). `SessionStore` gains `UpdateAuthenticatedAt(ctx, id string, at time.Time) error` — a dedicated method, not folded into T503's still-to-come `TouchSession(ctx, id, lastSeen, idleExpires)`, so the two events (step-up re-auth vs. liveness heartbeat) can't be silently conflated by a shared call (see the T501 Decisions row for the full reasoning). `storetest/session.go` extended in the same commit: `newSession` now sets `AuthenticatedAt`/`Method`, `CreateSessionRoundTripsByTokenHash` asserts both round-trip, and two new subtests (`UpdateAuthenticatedAtStampsTheStoredSession`, `UpdateAuthenticatedAtUnknownIDReturnsErrSessionNotFound`) pin the new method's contract; `memstore.SessionStore.UpdateAuthenticatedAt` and the three in-package `sulis_test.go` session-store doubles (`memSessionStore`, `observingSessionStore`, `sharedSessionStore`) implement it too (`failSessionStore` gets it for free via embedding `*memSessionStore`) — `memstore` stays 100%-suite-passing. `gitnexus impact` (upstream) before editing: `SessionStore`, `completeFirstFactor`, `CompleteTwoFactor` all LOW risk (0–1 direct dependents); `createSession`/`issueSessionForUser` LOW risk (1 direct dependent each); `Session`/`Register` returned ambiguous matches (a README section and a `LoginResult.Session` field share the name) but every candidate topped out at LOW. No HIGH/CRITICAL risk anywhere, so no warning was needed. `gitnexus detect_changes` afterward reported `risk_level: high` (43 changed symbols across 11 files, 6 affected processes) — verified via `git diff` that the alarming half is the same line-shift false-positive pattern T303/T304/T305/T402 already documented for this tool: every one of `errors.go`'s pre-existing sentinels, all five other `SessionStore` methods, `totp.ConfirmEnrollment`, three README sections (Password Reset/Magic Link/Two-Factor), and two `sulis_test.go` test functions/helpers were flagged purely because a real edit above them in the same file shifted their line numbers — each confirmed byte-for-byte unchanged in the diff. The five genuinely-affected `CompleteFirstFactor → *` processes are exactly `completeFirstFactor`/`createSession`'s real signature change, expected and intended. README: new "Step-up authentication" subsection under Core Flows naming every operation that should be gated (TOTP enroll/replace, passkey add/remove, 2FA disable, email change, recovery-code regeneration) with example wiring; root method list gained `RequireRecentAuth`/`ReAuthenticate`; Store Contracts' `SessionStore` bullet documents `UpdateAuthenticatedAt` in house MUST/SQL style. `totp.Service.Enroll`/`ReplaceEnrollment`'s "forthcoming `RequireRecentAuth`, T501" GoDoc forward-references now point at the real `sulis.Sulis.RequireRecentAuth`/`ReAuthenticate`. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 6 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues). Root `sulis` coverage 86.8% → 86.6% (new rare store/crypto error-propagation branches in `stepup.go`/`createSession` went untested, consistent with the pre-existing `VerifyPassword`/`ChangePassword` baseline — not chased further); `memstore` stays 100.0%. **Phase 5: 1/10 tasks done.** |
| 2026-08-18 | T502 | Account disable and lockout, per Appendix A. New `status.go`: `DisableUser(ctx, userID, reason)` stamps `User.DisabledAt`/`DisabledReason` via `updateUserWithRetry` and revokes every session for the account; `EnableUser(ctx, userID)` reverses the stamp (not the lockout fields — a separate mechanism, see below). New sentinels `ErrAccountDisabled`/`ErrAccountLocked` match Appendix A's ratified messages exactly. `User` gains `DisabledAt *time.Time`, `DisabledReason string`, `LockedUntil *time.Time`, `FailedLoginAttempts int` — plain fields, no `UserStore` interface change (the field-vs-Metadata and no-new-store-method choices are both recorded as T502 Decisions rows, discharging the brief's two explicit "decide ... record as Decisions row" instructions). New unexported `accountStatus(user) error` (checked by `completeFirstFactor`, `issueSessionForUser`, and `CompleteTwoFactor`, extending the brief's file list to include `twofactor.go` — see the Decisions row for why); `VerifyPassword` inlines the equivalent checks itself, split across its wrong/right-password branches so status is revealed only after a correct password (the account-status oracle discipline the brief demands — a wrong password against a disabled account still returns plain `ErrInvalidCredentials`); `ValidateSession` checks `DisabledAt` only, deliberately not `LockedUntil` (see the Decisions row for why a temporary lockout must not retroactively kill an already-issued session). Optional automatic lockout: `WithFailureLockout(threshold, baseBackoff, maxBackoff)`, default off (`FailureLockoutThreshold` 0) — `recordFailedLogin`/`clearFailedLogins` (both best-effort, via `updateUserWithRetry`, swallowing store errors so bookkeeping failures never change the caller-visible result) and the pure, unit-tested `lockoutBackoff(base, max, excess)` (doubling per failure past threshold, capped, overflow-safe by clamping the shift to `[0,32]` and clamping any zero/negative result to `max`). TDD: all new tests (`TestDisableUserBlocksLogin`, `TestValidateSessionRejectsDisabledAccountsExistingSession`, `TestDisableUserRevokesAllSessions`, `TestEnableUserRestoresLogin`, `TestDisableUser/EnableUserUnknownUserReturnsErrUserNotFound`, `TestAccountLockBlocksLoginUntilDeadlinePasses`, `TestVerifyPasswordChecksAccountStatusOnlyAfterPasswordVerifies`, `TestCompleteTwoFactorRejectsDisabledAccount`, `TestIssueSessionUncheckedRejectsDisabledAccount`, `TestRedeemMagicLinkRejectsDisabledAccount`, `TestFailureLockoutDisabledByDefault`, `TestWithFailureLockoutLocksAfterThreshold`, `TestLockoutBackoffGrowsExponentiallyAndCaps`) written against the unchanged `User`/`errors.go`/`sulis.go` first — confirmed red via `go vet` compile failure (`u.DisabledAt undefined`), this series' established compile-failure-as-red convention — then implemented. Two new test helpers, `disableUserDirect`/`lockUserUntil` (mirroring the existing `verifyUserEmail`/`unverifyUserEmail` pattern), let the two brief-mandated isolation tests stamp `DisabledAt`/`LockedUntil` directly, decoupled from `DisableUser`'s session-revocation side effect and from whatever triggers a lock in practice. Mutation-tested both properties the brief named by name: (1) removing `ValidateSession`'s `DisabledAt` check fails `TestValidateSessionRejectsDisabledAccountsExistingSession` exactly as predicted (`ValidateSession error = <nil>, want ErrAccountDisabled`); (2) removing the `time.Now().Before(...)` half of the lockout-expiry check (leaving a bare `LockedUntil != nil`) fails both `TestAccountLockBlocksLoginUntilDeadlinePasses` and `TestWithFailureLockoutLocksAfterThreshold`'s recovery step exactly as predicted. Also mutation-tested the new `storetest` coverage: removing the `DisabledAt`/`LockedUntil` pointer-copy lines from `memstore.cloneUser` fails the new `DisableAndLockFieldsRoundTripAndAreNotAliased` subtest with the predicted aliasing message. All three mutations reverted after confirming; `git status`/`go build` clean afterward each time. `storetest/user.go` extended in the same commit (`DisabledReason`/`FailedLoginAttempts` added to `assertUserMatches`; new `DisableAndLockFieldsRoundTripAndAreNotAliased` subtest covering round-trip, pointer-aliasing, and clear-back-to-nil for both pointer fields) and run green against `memstore`; `memstore.cloneUser` and `sulis_test.go`'s `cloneTestUser` both updated to clone the two new pointer fields. `gitnexus impact` (upstream, summaryOnly) before editing: `VerifyPassword`/`ValidateSession`/`completeFirstFactor`/`issueSessionForUser`/`CompleteTwoFactor` all LOW risk; `setPassword` HIGH risk (3 direct dependents) but never edited this task (see the Decisions row) — flagged per the project's HIGH-risk warning rule, no warning needed for the symbols actually changed. `gitnexus detect_changes` (scope `all`) afterward reported `risk_level: high`; verified via `git diff` that most of the flagged breadth is the same line-shift false-positive pattern T303/T304/T305/T402 already documented (confirmed zero occurrences via `grep -c` for each flagged-but-unedited symbol), with the `UserStore` interface's flag being a real but doc-comment-only change; see the Decisions row for the full breakdown, including `ValidateSession`'s real edit not appearing in the tool's output at all. README gained a new "Account disable and lockout" Core Flows section (disable/enable semantics, the ValidateSession-matters-most framing, the oracle-ordering rule, the `ValidateSession`-doesn't-check-`LockedUntil` split, and the deliberate off-by-default lockout rationale), the method list, the `UserStore` Store Contracts bullet, and the no-aliasing paragraph. Two scope boundaries recorded in Deferred rather than silently applied: `ReAuthenticate` doesn't check `accountStatus` (not an issuance path, and `stepup.go` isn't in the file list), and lockout bookkeeping isn't cleared by `ChangePassword`/`ResetPassword`/`SetInitialPassword` (not requested, and `setPassword` is the HIGH-impact shared helper flagged above). Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 6 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues). Root `sulis` coverage 86.6% → 88.1%; `memstore`/`passkey`/`recovery`/`totp` unchanged. **Phase 5: 2/10 tasks done.** |
| 2026-08-18 | T502 (fix round 1) | Task review upgraded the "lockout not cleared by password reset/change" Deferred row to an IMPORTANT finding. Fixed: `setPassword`'s existing `updateUserWithRetry` mutate callback (`sulis.go`) now also clears `FailedLoginAttempts`/`LockedUntil` on success, alongside the password-hash write it already performs — no extra store round trip, and it covers `ChangePassword`, `ResetPassword`, and `SetInitialPassword` with one change, since all three route through it. `DisabledAt`/`DisabledReason` are deliberately left untouched: disabling is an operator's decision, reversed only by `EnableUser`, never by proving control of the password. Without the fix, an attacker could lock a victim's account with nothing but repeated wrong guesses, and the victim's own recovery via `ResetPassword`'s out-of-band token — a stronger identity proof than a login password — would not lift the lock until the (recommended-long) `maxBackoff` passed, worsening the exact denial-of-service risk `WithFailureLockout`'s default-off posture exists to limit. TDD: new tests `TestResetPasswordClearsLockoutButNotDisable` (also proves the `DisabledAt` distinction directly — locks then disables the same account, and confirms the reset lifts the lock but the disable survives it, requiring a fresh `EnableUser`), `TestChangePasswordClearsLockout`, and `TestSetInitialPasswordClearsLockoutFields` (passwordless account; a no-op-on-zero-values check, since ordinary use can't put a nonzero `FailedLoginAttempts` on a passwordless user) written against the unfixed `setPassword` first, confirmed to fail with `sulis: account locked` on all three, then the fix applied. Mutation-tested: removing the two new lines from `setPassword`'s mutate callback reproduced the identical predicted failure on all three tests; reverted and confirmed green again. `gitnexus detect_changes` (unstaged) afterward: `setPassword` genuinely touched, the six affected processes are exactly `ChangePassword`/`ResetPassword`/`SetInitialPassword`'s `HashPassword`/`UpdateUserWithRetry` steps (the expected blast radius the earlier upstream-HIGH `gitnexus impact` rating warned about, now materialized as a real reviewed change), and the four flagged `UserStore` symbols confirmed via `grep -c` as the same doc-comment line-shift false positive the original T502 commit already produced (this fix round's doc updates to `LockedUntil`/`FailedLoginAttempts`'s field comments in `user.go` shifted lines again). README, `config.go`'s `WithFailureLockout` doc comment, and `user.go`'s `LockedUntil`/`FailedLoginAttempts` field comments all updated to state the clearing behavior and the `DisabledAt` distinction explicitly. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 6 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues). Root `sulis` coverage 88.1% → 88.2%. |

---
| 2026-08-18 | T503 | Session visibility and lifecycle, per Appendix A (which already named `TouchSession`, `ListUserSessions`, `DeleteUserSessionsExcept` on `SessionStore` at ratification — T503 is the task that finally implements them). `Session` gains `LastSeenAt time.Time`, `IdleExpiresAt *time.Time`, `IP string`, `UserAgent string`. New `SessionStore` methods per Appendix A's shape; new `Sulis.ListUserSessions(ctx, userID) ([]Session, error)` (strips `TokenHash`) and `Sulis.RefreshSession(ctx, session) (*Session, string, error)` (new ID, preserves `AuthenticatedAt` — see the two Decisions rows). New `WithIdleTimeout(d) Option`, zero disables (default). `createSession` grew a `ri RequestInfo` parameter, threaded from `Register`, `completeFirstFactor` (`Login`/`RedeemMagicLink`), and `CompleteTwoFactor`; `IssueSession`/`IssueSessionUnchecked` keep their Appendix A signatures unchanged and pass `RequestInfo{}` (see the Decisions row on why no deviation). `ValidateSession` now checks `IdleExpiresAt` (before `ExpiresAt`, same `ErrSessionExpired`/delete outcome) and, on success, throttle-touches `LastSeenAt`/`IdleExpiresAt` via `TouchSession` — only once the session's current `LastSeenAt` exceeds `sessionTouchInterval()` (`IdleTimeout/4` when idle expiry is configured, else a fixed 5 minutes; see the Decisions row for the cost rationale) — best-effort, so a failed touch write never fails validation itself. Also discharged the T501 Deferred row assigned here as a candidate: `ReAuthenticate` now calls `accountStatus` (after rate-limiting, before the password comparison — no oracle-ordering concern here, unlike `VerifyPassword`, since the caller already holds a valid `*Session` for this exact account). TDD: wrote all nine new tests (`TestSessionsRecordRequestInfoAtIssuance`, `TestListUserSessionsStripsTokenHash`, `TestListUserSessionsScopedToTheGivenUser`, `TestValidateSessionRejectsPastIdleExpiryBeforeAbsoluteExpiry`, `TestValidateSessionAcceptsSessionBeforeIdleExpiry`, `TestValidateSessionThrottlesTheLastSeenTouch`, `TestRefreshSessionRotatesTokenAndPreservesAuthenticatedAt`, `TestReAuthenticateRejectsDisabledAccount`, plus `storetest/session.go`'s ten new subtests) against the unchanged `Session`/`SessionStore`/`sulis.go` first — confirmed red via `go build` compile failure (`got.LastSeenAt undefined`, `store.TouchSession undefined`, etc.), this series' established compile-failure-as-red convention — then implemented. The idle-expiry tests and the touch-throttle test both back-date `LastSeenAt`/`IdleExpiresAt` directly in the test double's stored map rather than sleeping, mirroring T502's `lockUserUntil` pattern; the throttle test uses a new `touchCountingSessionStore` (wraps `memSessionStore`, counts `TouchSession` calls) to assert zero writes immediately after issuance, exactly one after backdating past the interval, and still one on an immediate follow-up call. Mutation-tested six properties: (1) removing `ValidateSession`'s `IdleExpiresAt` check — `TestValidateSessionRejectsPastIdleExpiryBeforeAbsoluteExpiry` failed exactly as predicted (`error = <nil>, want ErrSessionExpired`); (2) removing `Sulis.ListUserSessions`'s `TokenHash = ""` loop — `TestListUserSessionsStripsTokenHash` failed with the real hash value where empty was wanted; (3) making `RefreshSession` stamp `AuthenticatedAt: now` instead of preserving it — `TestRefreshSessionRotatesTokenAndPreservesAuthenticatedAt` failed exactly as predicted; (4) replacing the throttle condition with `if true` — `TestValidateSessionThrottlesTheLastSeenTouch` failed on the very first assertion (`touches = 1, want 0`); (5) removing `ReAuthenticate`'s new `accountStatus` call — `TestReAuthenticateRejectsDisabledAccount` failed (`error = <nil>, want ErrAccountDisabled`); (6) removing `memstore.cloneSession`'s new `IdleExpiresAt` clone — `storetest`'s `TouchSessionIdleExpiresAtIsNotAliased` failed with the aliasing message, run against `memstore` directly. All six reverted after confirming; `go build`/`go test` clean afterward each time. `storetest/session.go` extended in the same commit: `newSession` now sets `LastSeenAt`/`IP`/`UserAgent`; `CreateSessionRoundTripsByTokenHash` asserts all three round-trip; ten new subtests cover `TouchSession` (update, clear-back-to-nil, pointer-non-aliasing, unknown-ID), `ListUserSessions` (scoping, empty-is-not-an-error, non-aliasing), and `DeleteUserSessionsExcept` (removes others only, unknown `keepSessionID` still removes the rest, matching-nothing is not an error) — all run green against `memstore` (`cloneSession` updated to clone the new `IdleExpiresAt` pointer). The three in-package `sulis_test.go` session-store doubles (`memSessionStore`, `observingSessionStore`, `sharedSessionStore`) updated too (`failSessionStore` gets the new methods free via embedding `*memSessionStore`); `cloneTestSession` clones `IdleExpiresAt`. `gitnexus impact` (upstream, `summaryOnly`) before editing on every symbol touched (`Session`, `SessionStore`, `ValidateSession`, `createSession`, `completeFirstFactor`, `issueSessionForUser`, `IssueSession`, `IssueSessionUnchecked`, `ReAuthenticate`, `defaultConfig`, `Login`, `RedeemMagicLink`, `CompleteTwoFactor`, `Register`): all LOW risk (0–1 direct dependents, all non-test) — no HIGH/CRITICAL warning needed. `gitnexus detect_changes` (scope `all`) afterward reported `risk_level: critical` (53 changed symbols, 12 files) — verified via `git diff`: the two non-code files it flagged (`AGENTS.md`/`CLAUDE.md`, a stale `.gitnexus` re-index touching only the auto-generated symbol-count banner) were reverted, not committed; several flagged `memstore/session.go` methods (`GetSessionByTokenHash`, `DeleteSession`, `CleanExpired`, `Len`, `UpdateAuthenticatedAt`) and `sulis.go` methods (`createTokenForUser`, `createTokenForUserWithEmail`) confirmed byte-for-byte absent from the diff — the same line-shift false-positive pattern T303/T304/T305/T402/T501/T502 already documented, this time from inserting new methods above them in the same files. The genuinely-changed symbols are exactly this task's real edits: `SessionStore`'s three new methods plus `Session`'s four new fields (inherently wide-reaching, since the interface has four implementations to update) and the `RequestInfo`-threading touches to every `createSession` call site. README: new "Session visibility and lifecycle" Core Flows section (`ListUserSessions`'s `TokenHash`-stripping property named as the property to test first when implementing a custom store, `RefreshSession`'s new-ID/preserved-`AuthenticatedAt` contract, the "compose `ListUserSessions`+`RevokeSession`, or call `DeleteUserSessionsExcept` directly on your own store" guidance for "sign out everywhere else"); a new paragraph under "ValidateSession" for `WithIdleTimeout` semantics and the touch-throttle cost rationale; "Step-up authentication" and "Account disable and lockout" sections both gained a sentence on `ReAuthenticate`'s new `accountStatus` check; `SessionStore` Store Contracts bullet rewritten in house MUST/SQL style for all three new methods; root method list gained `ListUserSessions`/`RefreshSession`; the no-aliasing paragraph names `Session.IdleExpiresAt` alongside the existing pointer fields. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 6 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues). Root `sulis` coverage held at 88.2%; `memstore` held at 100.0%. **Phase 5: 3/10 tasks done.** |
| 2026-08-18 | T503 (fix round 1) | Task review upgraded a CRITICAL finding on `RefreshSession`: the original commit created the new session row before deleting the old one and discarded the delete's error, so a caller holding a stale `*Session` from before a revocation (`RevokeSession`, `RevokeAllSessions`, or an eviction via this task's own `ListUserSessions`/`RevokeSession` device screen) could call `RefreshSession` and mint a brand-new working session anyway — un-evicting themselves. A second, independent bypass rode along: `RefreshSession` never checked `accountStatus`, so a disabled account whose session row happened to survive `DisableUser`'s own (possibly failed) revocation could still refresh into a fresh one. **Fixed both, in order:** `RefreshSession` now calls `DeleteSession` FIRST and returns its error verbatim (fail closed) if it fails, then reloads the user and checks `accountStatus`, and only then mints the new row — so a disabled-account refresh attempt still burns the caller's old session on its way to `ErrAccountDisabled`, the same "burn first" direction `consumeToken`/`ConsumeChallenge` already take. New tests (`sulis_test.go`): `TestRefreshSessionAfterRevokeSessionFailsClosed` (revoke, then refresh the stale `*Session` → `ErrSessionNotFound`, session count unchanged — this is the test that actually catches the original ordering bug), `TestRefreshSessionAfterDisableUserFailsClosed` (the CRITICAL finding's literal scenario — `DisableUser`'s own revocation already removed the row, so this is caught by the same delete-first check), and `TestRefreshSessionRejectsDisabledAccountWhoseSessionSurvived` (uses T502's `disableUserDirect` to isolate the case where the session row survives disabling, forcing `accountStatus` — not the delete-first check — to be the one that catches it; also asserts the old row is still burned via `ValidateSession(oldToken)` → `ErrSessionNotFound`). The existing `TestRefreshSessionRotatesTokenAndPreservesAuthenticatedAt` extended per task review's Minor 2 to assert `IP`/`UserAgent` carry over from `Register`'s `RequestInfo` (both the GoDoc and the README already promised this; nothing had pinned it). Mutation-tested both halves independently: (1) restored the original create-then-delete-with-discarded-error order — `TestRefreshSessionAfterRevokeSessionFailsClosed` failed exactly as predicted (`error = <nil>, want ErrSessionNotFound`), while `TestRefreshSessionAfterDisableUserFailsClosed` still passed (that scenario doesn't depend on ordering, since `DisableUser` already removed the row) — confirming the isolated revoke test is the one that actually exercises fix 1; (2) dropped the `accountStatus` call (kept `GetUserByID` for the not-found case) — `TestRefreshSessionRejectsDisabledAccountWhoseSessionSurvived` failed exactly as predicted (`error = <nil>, want ErrAccountDisabled`), while `TestRefreshSessionAfterDisableUserFailsClosed` still passed, confirming the two fixes are independently load-bearing and each has a test that catches only its own removal. Both mutations reverted after confirming; `go build`/`go test` clean afterward each time. Also added the Minor-1-flagged missing Decisions row: `RefreshSession` carries `IP`/`UserAgent` forward from the caller-supplied stale session rather than re-deriving them (Appendix A gives the method no `RequestInfo`), with the consequence stated plainly — a repeatedly-refreshed long-lived session can show a stale IP/UserAgent in a device listing even while `LastSeenAt` looks current. `gitnexus detect_changes` (scope `unstaged`) was consulted but its output looked stale/inconsistent against the current tree (a prior `.gitnexus analyze` attempt this session had already failed with an internal `FTS index ... is inconsistent` error, unrelated to this change and not investigated further as out of scope); relied on manual `git diff` review of both changed files instead, which confirmed only `session.go` (the `RefreshSession` reorder) and `sulis_test.go` (the four test changes) were touched, nothing else. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 6 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues). Root `sulis` coverage held at 88.2%; `memstore` held at 100.0%. |
| 2026-08-18 | T504 | Rehash on login, per C3. New unexported `needsRehash(encoded string, want Argon2Params) bool` (`password.go`): decodes the stored hash's params via the existing `decodeHash` and compares only the three Argon2 cost dimensions (Memory, Iterations, Parallelism) against `want`; a decode failure returns `false` (see the Decisions row). New unexported `(*Sulis).rehashPassword(ctx, user, password)` (`sulis.go`), called from `VerifyPassword` right after the `if !ok` check, only when `needsRehash(user.PasswordHash, s.cfg.Argon2)` is true: re-derives the hash from the just-verified plaintext with the CONFIGURED params and writes it through `updateUserWithRetry`, whose mutate callback guards on `u.PasswordHash == verifiedHash` before overwriting — the same discipline `ChangePassword`'s `verifyOld` guard uses, closing the binding TOCTOU constraint. Every failure inside `rehashPassword` is swallowed (best-effort; see the Decisions row); three `// TODO(T509): emit rehash event` markers left for the not-yet-landed event sink. No public signature changed. TDD: six failing tests written first (`TestNeedsRehash` in `password_test.go`; `TestLoginUpgradesWeakStoredHash`, `TestLoginRehashRetriesOnConcurrentUpdateConflict`, `TestFailedLoginNeverRehashes`, `TestLoginRehashWriteFailureDoesNotFailLogin`, `TestLoginDoesNotRehashWhenStoredParamsAreStrongEnough`, `TestRehashDoesNotClobberConcurrentPasswordChange` in `sulis_test.go`) against the unchanged `password.go`/`sulis.go` — confirmed red via `go vet` compile failure (`undefined: needsRehash`), this series' established compile-failure-as-red convention — then implemented. New fixture `weakerArgon2Params` (next to `testArgon2Params`, weaker still) plus two new store doubles mirroring the existing `failUpdateUserStore`/`touchCountingSessionStore` pattern: `updateCountingUserStore` (counts `UpdateUser` calls, proves the strong-enough-already path writes nothing) and reuse of `failUpdateUserStore` for the best-effort-write test. The concurrent-update-conflict test reuses the `beforeUpdate`-hook interleaving technique to land an unrelated write mid-retry (asserting the retry preserves it and still lands the upgrade, `Version` advancing by ≥2); a second interleaving test lands a complete concurrent `ChangePassword` in the same window and asserts the rehash backs off rather than resurrecting the replaced password. Mutation-tested three properties: (1) `needsRehash` forced to always return `false` — both the upgrade test and the concurrent-retry test failed exactly as predicted; (2) the rehash call moved before the `if !ok` check — `TestFailedLoginNeverRehashes` failed exactly as predicted; (3) the guard's `u.PasswordHash != verifiedHash` condition short-circuited with `false &&` — `TestRehashDoesNotClobberConcurrentPasswordChange` failed exactly as predicted (the old password still verified afterward). All three reverted after confirming; `go build`/`go vet`/`go test` clean afterward each time. Found and fixed one staticcheck finding introduced by this task's own test code before committing: S1005 unnecessary blank-identifier assignment in a new test's map-lookup assertion. `gitnexus impact` (upstream, `summaryOnly`) on `VerifyPassword` before editing: 1 direct dependent (`Login`), LOW risk. `gitnexus detect_changes` (scope `unstaged`) afterward reported `risk_level: high` (10 changed symbols, 4 files); verified via `git diff` that `decodeHash`, `ChangePassword`, `SetInitialPassword`, and `checkPasswordPolicy` are byte-for-byte unchanged — the same line-shift false-positive pattern T303/T304/T305/T402/T501/T502/T503 already documented, each sitting below this task's real insertion in the same file. README gained a paragraph under "Login and VerifyPassword" documenting the upgrade path and its best-effort write. Full gate green: `gofmt -l .` empty, `go build ./... && go vet ./... && go test -race -count=1 ./...` pass (all 6 packages), pinned `staticcheck@2026.1` and `gosec@v2.28.0` both clean (0 issues). Root `sulis` coverage 88.2% → 88.4%; `memstore`/`passkey`/`recovery`/`totp` unchanged. **Phase 5: 4/10 tasks done.** |

## Finding dispositions

*Filled in T704.* All 33 audit findings, each marked closed, deferred (with reason), or obsoleted.
